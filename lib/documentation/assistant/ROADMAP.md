# Apogee — Roadmap

How we work: features get discussed in chat, written up here (and in [SPEC.md](SPEC.md)), given their own document in [`backlog/`](../backlog/README.md) when they're specced, then an agent takes the backlog item and builds it straight from that document (there is no separate TODO file — the backlog is the queue). When work ships it's recorded in detail in [MILESTONES.md](MILESTONES.md); this file stays high-level — the running board of themes per release, plus what's next.

**Version scheme:** feature releases are `v0.x.0`, patch releases are `v0.x.y`; development happens on a branch named for the upcoming release (currently `v0.1.0`) and merges into `stable`. Checked boxes are shipped; the detailed write-up of each is in MILESTONES.md.

---

## Shipped releases

*(Nothing shipped yet — the first tagged release starts this board.)*

---

## In progress

### v0.1.0 — The C++ harness (walking skeleton → four backends → install contract)

The first release, planned 2026-08-24 from Ommi's documentation (see [SPEC.md](SPEC.md) → Background for the lineage and divergences): a single self-contained C++ binary shipping `apogee complete` and `apogee chat` over all four backends — Anthropic, OpenAI, and Google called directly with streaming, native tool use, and typed thinking display, plus in-process llama.cpp — on top of the config engine, harness core, and shared agent loop, with persistent resumable sessions, a single-status-line terminal UX, and a clean install contract (`apogee check` passes on a fresh keyless, modelless install). Twelve backlog items, in build order:

- [x] C++ project skeleton (CMake, tests, CI, CLI scaffold) — *shipped 2026-08-25, [Milestone A](MILESTONES.md#milestone-a--project-foundation)*
- [x] Config engine (typed loader + comment-preserving mutation)
- [x] Harness core (LLMProvider, message IR, router)
- [x] Anthropic backend (direct Messages API + SSE)
- [x] `apogee complete` — the walking-skeleton closer
- [x] Shared agent loop (Reporter seam, tools, ask_user)
- [x] Terminal UX layer (status line, thinking view, cliReporter)
- [x] `apogee chat` (REPL, sessions, context monitoring, logging)
- [x] Chat line editing (replxx: arrow keys, history, completion)
- [x] OpenAI + Google backends — *[Milestone I](MILESTONES.md#milestone-i--the-full-cloud-set)*
- [x] llama.cpp in-process backend — *[Milestone J](MILESTONES.md#milestone-j--local-inference)*
- [x] Install contract + `apogee check` + completions — *[Milestone K](MILESTONES.md#milestone-k--the-install-contract)*

See the [`backlog/`](../backlog/README.md) index for the full queue (priority-ordered; topmost claimable item = next to build).

**Every v0.1.0 item has shipped** (Milestones A–K), and **`v0.1.0` was tagged on 2026-09-22** — the first tag pushed, and therefore the first real exercise of the tag → build → publish pipeline rather than a syntactic one. The procedure it settled is written down in [DEVELOPER.md](DEVELOPER.md#cutting-a-release).

*Decisions taken along the way: config format, data-dir name, web-search strategy, in-process llama.cpp, local vision's home, and — 2026-09-01 — GitHub Releases as the distribution host, self-update deferred, macOS unsigned, and all six targets shipping from the first tag. See the milestones that consumed each.*

*Answered so far — 2026-08-24: the platform matrix (Linux/macOS/Windows on both ARM and x86, six targets — revised 2026-09-19 to five, the Intel Mac dropped) and the repo host (GitHub, making GitHub Releases/Actions the default). 2026-08-25, with the skeleton: **C++20** as the language baseline, **CMake + FetchContent** for build and dependencies, **Catch2 v3** for tests, **nlohmann/json + CLI11 + libcurl** as the standardized library picks, and **macos-arm64 as the only merge-blocking CI target** for v0.1.0.*

---

## Fast follow

The gated ring — specced with their own backlog documents, sequenced after v0.1.0 ships (gate convention: each also assumes the full v0.1.0 set):

- [x] **Claude-CLI backend** — persistent child, token-level streaming; the subscription-plan path *(shipped 2026-09-02, [Milestone L](MILESTONES.md#milestone-l--the-vendor-cli-family))*. It also builds the family's shared machinery: the child-process seam, the JSONL framer, and the typed event union that codex/gemini/ollama reuse.
- [x] **Stdio machine mode** — the CLI's JSONL event stream over stdin/stdout, so the GUI can power the executable directly, never over localhost *(shipped 2026-09-06, [Milestone M](MILESTONES.md#milestone-m--the-front-end-contract))*. The protocol is documented for front-end authors in [machine-mode.md](../reference/machine-mode.md).
- [x] **Vendor CLI backends — complete.** The subscription-plan path now exists for all four cloud vendors. **Split per CLI at grooming (2026-09-06)** into three items, all shipped 2026-09-06 *([Milestone L](MILESTONES.md#milestone-l--the-vendor-cli-family))*: **codex** (typed events, no deltas, a pinned read-only sandbox), **ollama** (the odd one out and the weakest backend in Apogee — no event stream, no persistent child, and a pre-flight that refuses rather than let the CLI start a server), and **gemini** (real token deltas, Apogee-chosen session ids, and a safety pin that needs two flags to hold). With this, SPEC's dual-path claim is true in full: every cloud vendor works under both a subscription plan and an API billing plan, chosen per backend entry.
- Local-model depth — **split into four at grooming (2026-09-06)**. **Model operations shipped 2026-09-07** *([Milestone N](MILESTONES.md#milestone-n--model-operations))*: one shared role resolver used by every surface, the `apogee models list/info/status` suite, and a real GGUF header reader that replaced a magic-bytes check which had been reporting "model loads" for files that cannot load. **Open acquisition shipped 2026-09-07** *(same milestone)*: `apogee models pull` fetches any user-named ref from Hugging Face or the user's Ollama store through a copy → verify → commit ladder that never leaves a half-downloaded model behind, and reports **which** checks ran rather than a bare "verified" — because with no allowlist most sources publish no digest at all. **Local multimodal shipped 2026-09-07** *([Milestone O](MILESTONES.md#milestone-o--local-multimodal))*: a local model with an `mmproj_path` reads images in-process through llama.cpp's mtmd — verified live — and the cross-surface `--image` guard gap is closed. **Acquisition completed the same day** *([Milestone N](MILESTONES.md#milestone-n--model-operations))*: in-process `quantize` (5 GiB F16 → 1 GiB Q4_K_M in 11 s), and Ollama's projector layer copied alongside the model so a vision model arrives ready to configure. The transform this item inherited from Ommi was **never built** — asking the registry showed Ollama already ships the projector as its own layer, so there was nothing combined to split. **Model profiles shipped 2026-09-07** *([Milestone P](MILESTONES.md#milestone-p--model-profiles))*: a per-family registry with an explicit resolution ladder, and a streaming reasoning filter that fixed an observed leak — Qwen was printing `<think></think>` straight into its answers. The characterization **contradicted the plan**: Gemma 3 ships a working chat template where Ommi's Gemma 4 shipped none, so porting rather than characterizing would have overridden something that worked. **Model profiles completed the same day** *([Milestone P](MILESTONES.md#milestone-p--model-profiles))*: the gate — a model that actually emits control tokens or channel headers — was opened by **gpt-oss-20b**, which emits both. Its framing was printing as the answer and its tool calls were printing instead of dispatching; a header filter, a control-token parser, and a suppress-then-parse gate fixed both, verified live end to end. Here the reference implementation's transcription turned out to be **right** — it had marked the same framing unverified because the GGUF it had would not load — and the run still refuted the obvious design: the analysis channel is a reasoning block, not framing to delete, so stripping headers alone would have moved the model's working into its answer. Still pending in this track: SafeTensors and dataset downloads.
- RAG — **the lexical floor shipped 2026-09-07** *([Milestone Q](MILESTONES.md#milestone-q--the-retrieval-floor))*: a SQLite/FTS5 chunk store, `apogee embed ingest|query|list|info|delete`, and `--rag` on `complete` and `chat` splicing retrieved excerpts into the outgoing request without ever writing them to persisted history. It needs **no model and no network** — verified live against Apogee's own docs with zero backends configured. Two findings came from running it: FTS5's implicit AND returns nothing for any real question (a question carries words its answer does not), and every hit reports which retriever scored it, because BM25 and cosine scales are not comparable. **The config layer followed on 2026-09-12** *(same milestone)*: an `embeddings:` section a collection is registered under by its first ingest — through the comment-preserving editor, proven byte-exact against the shipped template — carrying the chunking later ingests read back; and `auto_rag`, one collection retrieved from on every turn with no flag, read at turn build so a mid-session edit takes effect on the next question, always announced on the status line, and switched off for one run by `--rag ""`. **Embedding clients shipped the same day** *([Milestone R](MILESTONES.md#milestone-r--embedding-clients))*: OpenAI and Gemini embed over their endpoints and llama.cpp embeds in-process, each a capability the harness discovers on the entry rather than a type in a list — Anthropic, with no embeddings endpoint, answers no. Running the local path against a real model found a llama.cpp batching behaviour that pooled a text over a fragment of itself whenever a shorter text shared its batch (cosine 0.56 of the truth), fixed with a unified KV cache and recorded. **The retrieval matrix shipped 2026-09-13** *([Milestone S](MILESTONES.md#milestone-s--the-retrieval-matrix))*, completing the RAG track: vectors in the store with a per-collection model binding, cosine and hybrid (RRF, ranks never scores) search, ONE per-turn resolver every surface shares — flag > collection pin > auto, with every degradation reported and nothing ever claiming what did not run — and a rerank judge under a never-fail contract. The spend rule was the user's call: a whole collection is never vectorised through a paid embedder unless asked; a question against one already built is allowed. Verified live against gpt-oss as both embedder and judge, which is also where the judge's token budget was found too small for a reasoning model.
- [x] **`apogee serve` — the public inference plane shipped 2026-09-13** *([Milestone T](MILESTONES.md#milestone-t--the-public-inference-plane))*: an OpenAI-compatible HTTP server — `/v1/chat/completions` and `/v1/completions` streamed or not, `/v1/models`, `/v1/model/status`, `/health` — over the **same** agent loop every other surface runs, through a third Reporter adapter that turns loop events into SSE frames and opt-in meta-frames. Server-owned sessions are ordinary chat sessions on disk, resumable with `apogee chat --resume`, compacted before they overflow, and evicted from memory on a TTL. Vendor-CLI backends are refused by type; a non-loopback bind fails closed behind `--allow-remote`; and the listening-socket invariant now has its one reviewed exception, enforced per object file. **Server deployments only**: remote clients (mobile/desktop) making REST calls to the executable on a server; never a localhost backend for a local front-end (decided 2026-08-24). Verified live with a stock OpenAI client library against gpt-oss. The reference for client authors is [http-api.md](../reference/http-api.md).
- [x] **The admin plane shipped 2026-09-13** *([Milestone U](MILESTONES.md#milestone-u--the-control-plane))*: `/v1/admin/*` on the same listener, behind a per-install `0600` bearer token that is header-only and compared in constant time, with the gate running before routing; a leaf events bus feeding one named-SSE lifecycle stream with a heartbeat; an async-job registry where cancel wins; the first CRUD slices — backends, the three role pointers, `config format` — through the one config-mutation path, **byte-identical** to the CLI; and a CLI↔HTTP parity test that classifies *every* subcommand and fails on one it has not seen (it caught `config format` on its first run). The mutating control plane over the same server; the GUI never uses it.
- [x] **The provider credential store shipped 2026-09-13** *([Milestone U](MILESTONES.md#milestone-u--the-control-plane))*: a `0600` `credentials.json` beside the config with one slot per API-billing provider type, `apogee auth add|list|clear` (hidden prompt, `--stdin`, `--from-env`; never a key on the command line), **one key resolver** in a leaf `secrets/` package — the entry's `api_key` > the stored slot > the conventional variable from a snapshot taken once — that the factory, `check`, `models list` and `auth list` all call, and the loopback-only, body-only `PUT /v1/admin/auth/{provider}` twin. The metadata type has no key field; `cli.one_key_resolver` keeps the chain single. The three `[user]` calls were answered that day: a private file rather than the OS keychain, a configured `api_key` stays and wins, and git-forge slots wait for the agents work.
- **Tools and agents** — **split at grooming 2026-09-13** into three items, in build order. **The native toolsets shipped 2026-09-13** *([Milestone V](MILESTONES.md#milestone-v--the-native-toolsets))*: fs behind a component-wise sandbox, shell (gated, unlike Ommi's), git as a child with list arguments and a ref allow-list carrying the deterministic review diff, notes, and a RAG query through the one retriever resolver — all in-process, no Python — plus the `permissions:` schema, the `[y]es / [n]o / [a]lways / [s]ession` prompt, machine mode's permission question, `config set-permission` and its admin twin, and the rule that a surface with nobody to ask denies. **The MCP stdio client shipped 2026-09-13** *([Milestone W](MILESTONES.md#milestone-w--the-mcp-client))*: a from-scratch client for stdio servers whose tools join the loop as `mcp__<server>__<tool>`, gated unless the server marks them read-only; a server's stderr captured never inherited, a dead one failing at once with its tail, a bad one skipped never fatal; `apogee mcp create|list|test|enable|disable` and their admin twins through a shared scaffold core; and the in-binary `apogee __mcp-tools`, which serves Apogee's own read-only toolsets to any MCP client with nothing to install. **Analyze and agents shipped 2026-09-13** *([Milestone X](MILESTONES.md#milestone-x--agents-as-data))*, completing the track: agents as data run by the shared loop, a per-agent tool policy enforced as a filter over the registry (`read-only` never prompts, on any surface), structured output asked natively where a provider has a mode and validated client-side always with one corrective retry, an in-process renderer with `human_summary` last, branch review deterministic from flags on `analyze` and `chat`, `apogee agents` and its admin twins over one scaffold core, and `security-review` / `release-notes` / `merge-request` re-authored for the owned loop and compiled in. Vendor-CLI backends are refused by type, the user's call. The three items, in build order: **native toolsets** (fs, shell, git, notes and a RAG-query tool in-process, the permission gate's first real consumer and the home of the `permissions:` schema — no Python runtime), the **MCP stdio client** (`mcp__<server>__<tool>` in the same registry, stderr captured never inherited, a dead server failing fast, `apogee mcp`, and an in-binary server exposing Apogee's own read-only tools), and **analyze/agents** (agents as data run by the shared loop with per-agent tool policies replacing any vendor permission forwarding, provider-native structured output on every backend, deterministic branch review from flags, `apogee agents`, and the security-review / release-notes / merge-request agents re-authored for the owned loop). The merge-request agent ships in branch-review form; its forge-backed mode rides the forge-target decision below.
- **Knowledge layer + knowledge graphs — split at grooming 2026-09-13** into four items, in build order. **Knowledge records + capture shipped 2026-09-13** *([Milestone Y](MILESTONES.md#milestone-y--the-knowledge-layer))*: the canonical record with Ommi's full schema, the capture clerk as one provider-native structured-output call validated on every backend (the one piece that came out better than the original), archive rich / surface thin over a new chunk-metadata column with the raw conversation under a private layout row, `--dry-run` with zero footprint, chat's `/capture` with the loaded model as the clerk and `auto_capture` on a clean exit, and the two admin routes. **Query, lifecycle and the HTTP draft → refine → store contract shipped 2026-09-19** *(same milestone)*: `knowledge query` through the one retriever resolver, defaulting to shipped and filtering before the cut, the judge under its never-fail contract; `list`, `info --raw`, `link` and `status` as metadata edits that never re-embed, `delete`, `export` as a re-importable array or a Markdown report with `--anonymize` keeping the chain, and a vector-only `reindex` honest about a lexical collection; over HTTP the list/query, get, patch, delete and reindex routes, `draft: true` on capture, and a stateless `/refine` bounded at 2000 characters whose round trip is proven field-equivalent to a one-shot capture. **The graph build and retrieval-time expansion shipped 2026-09-19** *(same milestone)*: `apogee graph build|stats|show|delete` over a collection's own database -- one structured-output call per chunk, the closed type set and the caps enforced in host code, builds incremental and resumable on a (count, max chunk id, model) fingerprint, a metered default refused as the extractor unless named; every knowledge record a reserved `decision` node with `concerns` and `supersedes` edges, materialised deterministically on every build; expansion on every RAG surface seeded before the judge under a 1,500-codepoint transient budget and reported as `+N graph entities`; `knowledge query --graph` and `?graph=true`; the build as the admin plane's first async job. Running it end to end found that the chunk table's ids were reusable, which would have blinded the fingerprint to exactly the re-ingest it exists for; they are AUTOINCREMENT since. **The global layer shipped 2026-09-19** *(same milestone)*, completing the track: `apogee graph communities` -- deterministic label-propagation clusters, each summarised by one generation call and stored as an ordinary retrievable chunk, so "what are the main themes?" is answered by plain retrieval on any retriever, identity by exact member set so an unchanged cluster costs nothing; `graph dedupe`, merging same-type entities whose vectors agree, never on its own and never a decision node; **named graphs** (`graphs:` in the config, `config add-graph`/`delete-graph`) spanning several collections into one node per entity in their own database under `embeddings/graphs/`, rebuilt never absorbed, membership converging, a built one taking retrieval precedence for its members, resolved graphs-first everywhere with the collision ban in `check`; `embed ingest --graph` chaining the covering graph's build; and the eight admin twins. Two calls settled at grooming: a full graph build runs on a metered cloud backend only when that backend is named explicitly (a local extractor by default, mirroring the embedding spend rule), and every clerk — capture, refine, extraction — is one structured-output call validated client-side.
- [ ] **Terminal markdown rendering** *(asked for 2026-09-23, specced the same day — [backlog item 23](../backlog/terminal-markdown.md))*: bold, italic, code, headings, lists, quotes, fenced code, links and tables rendered as an answer streams on a terminal, one open line redrawn in place and never the last column; a pipe, machine mode and the saved transcript keep the model's text byte for byte. Not a TUI — it styles the scrolling transcript. Two calls are the user's: a hand-written renderer or md4c, and whether code blocks are highlighted.

## Outer ring — scheduled for v0.1.0

- **Training and distillation** *(confirmed direction 2026-08-24; **split at grooming 2026-09-19** into three build-sized items, in build order, and **scheduled for v0.1.0 the same day** — the user's call)*. **Datasets, kits and the Python boundary shipped 2026-09-19** *([Milestone Z](MILESTONES.md#milestone-z--the-training-track))*: the `training` layout row; a virtual environment Apogee owns under it, created by `apogee train setup` or after a terminal prompt and never by seeding; four training kits and `prepare_dataset.py` compiled in and seeded like the bundled agents; a script runner that frames a driver's JSONL with the one framer, keeps a non-JSON line as a message, treats an error line as its own event and carries the exit code (three of the reference's silent gaps, closed); `apogee datasets prepare|create|synth|kits|list|info|delete|pull`, with synth through a named teacher running parallel batches with retries and exposed over HTTP as the plane's third job kind; and SafeTensors snapshots and dataset downloads through a tree ladder that verifies every shard against its published digest. **The run shipped 2026-09-19** *(same milestone)*: `apogee train run|eval|promote|rollback|versions|status` -- LoRA/QLoRA of a SafeTensors snapshot through `train_mlx.py` on Apple Silicon or `train_peft.py` on CUDA under a JSONL progress protocol proven on both sides (the parser against a golden fixture, the shipped drivers under stub modules on bare python3), the mock trainer in the binary so the whole chain runs with no Python; the eval gate at 100% with substring items and pairwise-judge items against the run's own untuned base under the never-fail contract; promotion as gate → fuse → convert (llama.cpp's own converter, vendored at the pinned revision and compiled in) → verify → register through the one config editor → the version ledger, with `max + 1` numbering and retention that never touches the active version; rollback that deletes nothing; the reads served under `/v1/admin/training/*` and every control action a carve-out. Reading the reference's drivers against today's libraries found three things that could not have run -- `mlx_lm.lora` wants a directory, the MLX driver rejected the `--mask-prompt` its own orchestrator passed, and trl removed the collator the PEFT driver imported -- each fixed in Apogee's. **Pipelines, regimes and the cycle shipped 2026-09-19** *(same milestone)*, **completing the track**: `apogee train pipeline run|resume|status` -- ordered stages, each a fresh LoRA on the previous stage's fused checkpoint under the cumulative 100% gate, the final stage left for `promote`, the manifest rewritten on every transition, resume from the first unpassed stage; `apogee train regime run` -- a teacher distilling a dataset per kit through the synth core, the kits as one gated pipeline, the last passing stage promoted; and `apogee train cycle run|status|halt|resume` -- one scheduler-invoked gated pass under a PID lock and a circuit breaker, a directory queue and the user's own sessions only with `log_consent: true` and never twice, the anchor-baseline dual gate, promotion through the same path `promote` takes, `halt`/`resume` replacing the reference's hand edit of its state file; the reads under `/v1/admin/training/cycle` and the pipeline kind under `/runs`, every control action a carve-out. The three items as specced: **datasets, kits and the Python boundary** (a dedicated venv under the data directory, never the system Python; the trainer scripts and six training kits compiled in and seeded like the bundled agents; `apogee datasets prepare|create|synth|pull`, with synth — teacher inference, not training — as an async admin job and the teacher a direct API or local call with rate-limit handling; SafeTensors snapshots and dataset downloads through the models item's ladder), **fine-tuning runs** (`apogee train run|eval|promote|rollback|versions|status` — LoRA/QLoRA through MLX on Apple Silicon and PEFT on CUDA as Python subprocesses speaking a JSONL progress protocol tested on both sides, substring and pairwise-judge gates at 100%, eval-gated promotion fusing the adapter and converting to a versioned GGUF registered as a `llamacpp` backend through the one config editor, retention and rollback), and **pipelines, regimes and the cycle** (multi-stage pipelines with cumulative gates and fused-checkpoint chaining, `train regime` distilling a teacher across kits, and the scheduler-invoked continuous cycle with the anchor-baseline dual gate, consented session sources, and the circuit breaker). Settled at grooming: training control stays CLI-only over HTTP with reads served; and — the user's call — four kits ship now while the two tool kits wait for the in-text tool protocol as its own tools-track item, because the loop shows a local model no tool definitions today and Ommi's kits teach a protocol Apogee does not parse.

## Unspecced ideas

- **GUI sibling applications** *(committed direction 2026-08-24; planned home `lib/src/darwin|linux|windows`, one app per platform)* — graphical front-ends shipped alongside the harness, powering the CLI directly over stdin/stdout via the structured JSONL machine mode (never a localhost port; mutations shell out to the same CLI commands). Its contract now exists — see the [machine-mode protocol](../reference/machine-mode.md) (shipped 2026-09-06); the GUI itself still needs its own planning pass. No TUI, ever.

## Ideas / candidate features

- Plugin system (`plugin.yaml` overlay — deliberately left out of the initial plan; separable later on top of the config engine)
- **Self-update (`apogee update`)** — deliberately deferred out of v0.1.0 *(decided 2026-09-01)*, and now unblocked: the distribution host is settled and the release pipeline exists. It becomes the **third parity path** alongside the two installers, so it must go through `apogee check --fix` like they do rather than seeding anything itself. Needs its own backlog item; the background update notifier (TTY-only, 24h cache, subcommand skip-list) rides with it.
- Remote MCP transports + OAuth connectors (and the connector-driven review agents that ride them)
- **Forge integration for the review agents** — a GitHub and/or GitLab tool server for the merge-request agent's MR mode (read the MR, its discussions and diff; look up a ticket) and the forge credential slots deferred from the credential store (2026-09-13). **The forge target is the user's call** (GitHub, GitLab, or both) and gates this item; it would be the first user of the in-binary MCP server pattern beyond Apogee's own tools.

## Explicit non-goals

Ruled out on purpose (see [SPEC.md](SPEC.md) → Non-goals for the rationale):

- **Mandatory cloud dependency**
- **Bundled models**
- **TUI front-end** (the GUI is a committed sibling project over the HTTP planes — never in-binary)
- **Silent install drift**
