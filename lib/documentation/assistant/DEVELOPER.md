# Developer Guide

This document describes the layout of the Apogee repository and what each file does. For user-facing documentation, see [`README.md`](../../../README.md) (not yet written); for how to work in the repo, see [CLAUDE.md](CLAUDE.md).

---

## Quick start

```bash
lib/scripts/cicd.sh --test
```

The repo-wide entry point, and the one every CI runner calls: it builds each application for the host's native target and runs its suite. For the CLI alone, `make -C lib/src/cli test` does the same thing; `make -C lib/src/cli help` lists every target.

Requirements: CMake ≥ 3.25, a C++20 compiler, and git. `format` / `lint` additionally need `clang-format` / `clang-tidy` (Homebrew LLVM on macOS — Apple's toolchain ships no clang-tidy).

---

## One application, one build

**Each application under `lib/src/<app>/` is a self-contained CMake project.** Its presets, cmake helpers, pinned third-party code, style config, and build output all live beside its source. Nothing above `lib/src/<app>/` needs to know how that application compiles.

Today that is `lib/src/cli`. The GUI applications land later as siblings (`lib/src/darwin|linux|windows`), each owning its build the same way, and each joining the `APPS` list in `lib/scripts/cicd.sh`.

The one build-related file outside an app directory is `.github/workflows/ci.yml`, because GitHub requires workflows at `.github/workflows/`. It is deliberately a thin caller into `cicd.sh` for exactly that reason.

---

## Directory Layout

```
Apogee/
├── .github/
│   ├── workflows/ci.yml     — CI: three stages — clone llama.cpp and the suite per platform (in parallel), then build per platform; plus the PR-only `version bump` check (2026-09-22)
│   └── workflows/release.yml — Release: gate (version + already-released?) → build ×5 → publish; runs on a merge to stable or a v* tag (2026-09-22)
│                              (thin caller into cicd.sh; here only because GitHub requires it)
├── .claude/
│   └── skills/
│       ├── apogee-backlog-item/        — Repo-local skill: take the next backlog item per the docs-first process
│       ├── apogee-create-backlog-item/ — Repo-local skill: spec a new item into the backlog + index + roadmap
│       ├── apogee-document-update/     — Repo-local skill: pre-MR docs pass — reconcile every doc against the branch diff
│       └── apogee-pull-request/        — Repo-local skill: draft the MR description from the branch's docs evidence
├── .gitattributes           — Every text file is LF on every platform (Windows checkouts had rewritten
│                              the byte-exact shipped assets with CRLF, 2026-09-20)
├── .gitignore
└── lib/
    ├── documentation/       — Project documentation
    │   ├── assistant/       — Contributor docs (CLAUDE, SPEC, ROADMAP, MILESTONES, this file)
    │   └── backlog/         — The work queue: pending work, one document per item (see its README)
    ├── scripts/             — Repo scripts
    │   ├── cicd.sh          — CI/CD entry point: builds every app for any of the five release
    │   │                      targets (--platform linux|windows × x64|arm64, macos-arm64, or all;
    │   │                      non-native targets defer to the CI matrix, or fail with --no-defer), --fresh = clean-room
    │   │                      clone of github.com/QuantumCompiler/Apogee at that branch; --test, --clean;
    │   │                      --clone-llama is the first CI stage (the suite never runs on a runner)
    │   ├── cicd-completion.bash — Tab completion for cicd.sh's flags (source from your shell rc)
    │   └── ci-annotate.sh   — On a failed CI step, publishes the ctest summary and each failed
    │                          test's output as error annotations (public through the API; the log is not)
    └── src/                 — Application source, one self-contained project per app
        ├── cli/             — The CLI application
        │   ├── CMakeLists.txt   — Build root: standard, options, target wiring, link-policy assertion
        │   ├── CMakePresets.json— One preset per release target + `default` (host native)
        │   ├── Makefile         — Thin wrapper (build/install/test/lint/format/clean/fresh)
        │   ├── .clang-format    — Formatting rules (make format / make format-check)
        │   ├── .clang-tidy      — Static analysis, incl. the smart-pointer ownership gate
        │   ├── cmake/
        │   │   ├── ApogeeDependencies.cmake — FetchContent declarations + how to add a dependency
        │   │   ├── ApogeeLinkPolicy.cmake   — Configure-time assertion that nothing links the CLI executable
        │   │   └── ApogeeWarnings.cmake     — Shared warning flags for first-party targets
        │   ├── assets/          — Shipped data files: config.yaml (the starter config), the bundled
        │   │                      agents' prompts/ and schemas/, the capture, refine, community and extraction clerks' clerks/,
        │   │                      and training/ (the four kits and the three drivers) —
        │   │                      each held byte-identical to its compiled-in copy by a test
        │   ├── third_party/     — Vendored/pinned code, never edited in-tree (see its README); llama.cpp-convert/ is the converter compiled into the binary
        │   ├── scripts/         — generate_training_assets.py: regenerates the compiled-in training assets and the converter
        │   ├── build/           — Build output, one dir per preset (git-ignored)
        │   ├── source/          — main.cpp + one package dir per concern
        │   └── tests/           — Test dirs mirroring source/'s packages
        └── <darwin|linux|windows>/ — GUI applications, one per platform (future — down the road)
```

---

## `lib/src/cli/source/` — the CLI application

Two CMake targets, and the split between them is load-bearing:

| Target | Contents |
|---|---|
| `apogee_core` (static library) | Every capability. Tests link this; the future HTTP server will link this. It is the whole product minus an entry point. |
| `apogee` (executable) | A thin face: argv in, exit code out. Nothing else. |

That split is what makes surface parity structural rather than something a reviewer has to notice ([SPEC.md](SPEC.md) → Principles). It is enforced at configure time — `apogee_assert_link_policy()` in `lib/src/cli/CMakeLists.txt` fails the build if any target ever links `apogee`.

### Packages

| Path | Role |
|---|---|
| `main.cpp` | Entry point. Constructs a `RootCommand` and returns its exit code. |
| `version/` | Build identity — semantic version, git commit, build date, target — stamped in at configure time. `version.h/.cpp` |
| `platform/` | **The portability seam.** The one place platform `#ifdef`s are expected; everything else asks this header. `platform.h/.cpp` Since Milestone X also `run_foreground`: a child with the terminal's own stdio, the one documented exception to the captured-stderr rule, for `agents edit`'s `$EDITOR` and nothing on an inference path. Since Milestone Z's pipelines item also `current_process_id` and `create_exclusive_file` (one atomic create-or-fail: `O_CREAT\|O_EXCL`, `CREATE_NEW` on Windows), the two mechanisms the cycle's PID lock needs. |
| `ansi/` | Colour and styling, and the one function that decides whether to emit any (`resolve_color`). `ansi.h/.cpp` |
| `logger/` | Persisted chat sessions and the daily operational log. A session records *what was said*; the log records *what the program did*. A session records its `retriever` and `rerank` **settings** (never a per-turn resolution); a judge deleted since is dropped on resume with a warning. Since Milestone Y also `transcript_text`: a conversation as `User:`/`Assistant:` turns for a reader that is not a model client — the knowledge clerk — with system messages, tool results and empty turns left out. |
| `commands/` | The CLI scaffold: the `Command` interface, the registry, the root command, and the built-in commands. Since Milestone X also `analyze` and `agents`, and the doctor's `Agents` section: each bundled file present or a warning with `--fix` as the remedy, and every entry's files, schema (validated as draft-07), model, collection and servers. Since Milestone Y also `knowledge` (alias `kn`) over `knowledge_core`, the capture core every surface calls, and the doctor's `Knowledge` section: the collection counted with its text index verified, the raw archive private. `graph.h/.cpp` (Milestone Y): `apogee graph build [-m] [--dry-run] [--force] [--limit]` -- the extractor `-m` > the collection's `graph.extract_backend` > the extraction role > the default through the one role resolver, a metered default refused naming the three ways, a vendor CLI refused by type, entity vectors under the embedding spend rule, the first success writing `graph.enabled` through the editor -- and `stats`, `show [--chunks]`, `delete`. The doctor's `Graph` section: every collection whose `graph:` block says anything -- `extract_backend` must be configured (fail), an enabled graph that is not built warns, a built one reports its shape and stale sources. Since the fourth slice every `graph` subcommand resolves its `<name>` **graphs-first** (a `graphs:` entry is a named graph in its own database, built over its members through `GraphMembers`/`open_graph_members`, stats per member with a membership-drift note, `show` naming each chunk's collection, `delete` removing the file), plus `communities [-m] [--force] [--min-size] [--list]` and `dedupe [--threshold] [--dry-run]`; `run_graph_build` is the one build body `embed ingest --graph` chains to (the first `graphs:` entry listing the collection, else its own), and `validate_named_graph` the one rule set `config add-graph` and its admin twin share (a plain name, at least one member, **no collision with a collection name**, a configured extractor, knobs in range; an unknown member a warning). `config add-graph|delete-graph` in `config_cmd.cpp`. The doctor's `Graph` section also validates every `graphs:` entry: the collision ban and a missing member fail, built or not reported. Since Milestone Z also `datasets` (`prepare|create|synth|kits|list|info|delete|pull` -- the cores `create_dataset`, `resolve_teacher`, `make_teacher` and `effective_parallel` shared with the admin twins) and `train`: `setup` plus `require_python_env`, the one prompt-or-refuse rule every command that needs the environment calls; and since the run item `run` (the student through `resolve_student` -- a directory, or a name under `paths.hf_dir` or `models/`, a backend entry or a GGUF refused naming `--safetensors` -- the dataset through `resolve_dataset`, the trainer flag > manifest > config > auto, the manifest written `running` then rewritten, the status line from the progress protocol with a line every tenth on a pipe, Ctrl-C through a SIGINT handler that only flips the token), `eval` (the suite through `resolve_suite` -- a path, `training/suites/`, a prepared `.eval.jsonl`, or a kit's inline items -- the judge through `resolve_teacher` with the vendor-CLI refusal and `make_judge` at the 1024-token verdict budget, the untuned base as the baseline through the same runner), `promote` (every refusal before the expensive part: the gate, a non-llamacpp `--as`, an unknown or unsupported `--quantize`; the converter as a closure -- the mock's or the vendored script under the environment's interpreter with the `convert` set -- the header reader as the verifier, `models::quantize` as the quantizer; the config edit only after the GGUF exists and parses, the ledger only after the config), `rollback`, `versions`, `status`; and the doctor's `Training` section: the converter tree as one row, the trainer this host would use and its set, the `convert` set, and every ledger's consistency with the config. Since the pipelines item the promote body is one function (`promote_run` over `precheck_promotion`) that the `promote` command calls and hands to the regime and the cycle as a closure; `pipeline run|resume|status` (a spec by file or `training.pipelines:` name through `resolve_pipeline_spec`, the student and every stage's dataset and suite through the run item's resolvers, the console rendering a header per stage and the training ticks on the status line, resume finding the spec by the manifest's name or `--pipeline`), `regime run` (flags over a named entry or a spec file through `resolve_regime_spec` and `apply_regime_flags`, every refusal -- teacher, student, kits, judge, trainer, and the converter when promoting -- before the first teacher call), `cycle run|status|halt|resume` (the configured block or a refusal naming the three keys, the sessions loaded only when a source needs them, a failed or refused pass exiting non-zero for the scheduler's log), and `status` rolling up the cycle and the pipelines; the doctor's `Training` section grows every named pipeline's student and datasets, the cycle's configuration (an unknown pipeline or a non-`llamacpp` backend a failure) and a halted history with `cycle resume` as the remedy. |
| `harness/` | **The core.** The config engine (typed loader, `${ENV}` expansion, template, comment-preserving edits, on-disk paths), the **layout contract** (`layout.h` — the single declaration of what `~/.apogee/` contains), and the provider interface, canonical message IR, router, and context-window table. Includes nothing from `backends/` — enforced. Since Milestone X also `assets.h/.cpp`: the three bundled agents compiled in (prompts and schemas byte-identical to `assets/prompts` and `assets/schemas`, a test enforcing it), `seed_bundled_assets` called from the one seeding path and writing absent files only, `resolve_agent` (a config entry overrides a bundled definition of the same name) and `all_agents`, `resolve_agent_path` (a relative path against the data directory the config lives in), and `is_unmodified_bundled_asset` for the uninstall plan; and the layout rows `prompts`, `schemas`, `analyses`. Since Milestone Y also the `knowledge:` section (`KnowledgeConfig`: `auto_capture`, `db`, `collection()`) and the `knowledge` layout row (private, user data) with `knowledge_dir`/`knowledge_raw_dir`. Since Milestone Z the `training` layout row (private, user data) with `training_dir`/`training_datasets_dir`/`training_raw_datasets_dir`/`training_kits_dir`/`training_scripts_dir`/`training_venv_dir`; `assets_training.cpp` -- the four kits and `prepare_dataset.py` compiled in, `bundled_kits`/`bundled_training_scripts`, and `bundled_files()`, the ONE list of everything seeding materialises, which `seed_bundled_assets`, `is_unmodified_bundled_asset` (now answering for a directory whose every file is Apogee's) and the doctor's drift row all read; and `TrainingConfig` (`training.python`; since the run item `trainer`, `judge_backend`, `eval_suite_path`, `retain_versions` (3), `gate_mode` (hard), validated at load); the layout accessors `training_runs_dir`/`training_versions_dir`/`training_suites_dir` and, since the pipelines item, `training_pipelines_dir`/`training_regime_dir`/`training_cycle_dir` (subdirectories of the one row, created by first use); `PipelineSpec`/`PipelineStageSpec`, `RegimeSpec` and `CycleConfig`/`CycleSourceConfig` under `TrainingConfig` (`pipelines`, `regimes`, `cycle`), validated at load -- a stage without a name, dataset or suite, a bad method, a number out of range, an unknown source type, a `sessions` source without `log_consent: true` (the refusal names the two risks), a breaker below zero, a threshold outside `[0, 1]` all fail the load -- with `parse_pipeline_spec`/`parse_regime_spec` reading a spec file through the same node parser a config entry gets; `set_backend_model_path` in `config_edit.h/.cpp`, the section-scoped in-place replacement `promote` and `rollback` repoint an entry with; `assets_converter.cpp` -- llama.cpp's converter (`third_party/llama.cpp-convert/`) compiled in as `bundled_converter_files()`, chunked under MSVC's literal limit like every training asset since, both generated units written by `scripts/generate_training_assets.py`. |
| `models/` | Model files as artifacts, independent of any backend: reading them, verifying them, and acquiring them. `gguf_inspect.h/.cpp` — a self-contained GGUF **header** reader (architecture, container version, tensor counts, and a text-vs-vision split). Deliberately not llama.cpp's `gguf.h`: that library is off by default and absent from the merge-blocking CI target, so a reader behind it would leave `check` reporting nothing in the default build. Every self-declared length is bounds-checked, because the first thing this meets in the wild is a half-downloaded model. Since Milestone Z `acquire_file` (the ladder with the GGUF rung skipped, the sidecar saying so) and `acquire_tree` (a whole tree into a `.staging` directory, renamed into place last, a failure anywhere removing it), `list_repo_tree` over Hugging Face's `/tree` endpoint -- the one that publishes a per-file sha256 for LFS files -- with `HfRepoKind` for models and datasets, the snapshot and dataset file filters, and `snapshot.h/.cpp`: the `apogee-snapshot.json` record beside a SafeTensors snapshot, `is_snapshot_dir`, the architecture from `config.json`. |
| `backends/` | Every provider plus the infrastructure they share: `mock`, `anthropic`, `openai`, `google`, `llamacpp`, and the four vendor-CLI backends `claude_cli`, `codex_cli`, `gemini_cli`, and `ollama_cli`; the HTTP client and SSE parser for the direct-API ones; the `llama_runtime` seam, chat templates and token arithmetic for the local one; the JSONL framer and typed event union for the vendor-CLI ones. |
| `agentloop/` | **The shared loop.** `run()` drives model→tool→model behind a Reporter, plus `ask_user`, context measuring and history compaction, and transient splicing. `rag.h/.cpp` builds the retrieved-excerpt prefix that rides the transient path — so context reaches the model and never the saved session. Includes nothing from `backends/` — enforced. `embed_func.h/.cpp` is the one embedding seam; `retriever.h/.cpp` the **one per-turn resolver**; `rerank.h/.cpp` the judge under its never-fail contract. Since Milestone X also `structured.h/.cpp` (draft-07 validation over `pboettch/json-schema-validator`, the JSON extractor, the OUTPUT FORMAT block, and `run_structured`: one corrective retry, then the raw answer flagged `conforms: false`) and `review_context.h/.cpp` (the arbitrary-branch review: `/branch` parsing, the system note, the summary). Since Milestone Y `Options::side_request` marks every request of a run as not a turn of the caller's conversation, so a local backend runs it on its own context. `graph_context.h/.cpp` (Milestone Y) renders the retrieval-time expansion: `entity_line` (a decision node folds its branch marker in), `build_graph_section` -- the retrieved top-k and, on a lexical or hybrid turn, the query's own terms through the entity index seed a walk, rendered as `[Knowledge graph: <name>]`, entity lines, then triples, under a 1,500-codepoint budget with whole-line truncation. `rag.cpp` seeds it BEFORE the judge and appends it after the chunk list in the same transient message, so a judge that drops every chunk still leaves the section; a failure is a note, never the reason a turn loses its chunks; `RagResult::graph_entities` is what every surface reports. Since the fourth slice `graph_context` also holds **the precedence rule**: `graph_db_path` (`<embeddings_dir>/graphs/<name>.db`, derived data created by the first build), `named_graph_covering` (the first `graphs:` entry listing a collection -- built only, for retrieval; by config membership alone, for the ingest chain) and `resolve_turn_graph`, ONE decision for every surface: a built named graph covers its members (the member's own block untouched and resumed the moment the collection leaves), else the collection's own enabled block, else nothing; `build_graph_section_labelled` walks any graph store with (collection, chunk) seeds under the graph's own header. `RagTurn` carries the resolved store path, header and seed label; a turn has one collection, so one graph and one section. |
| `agent/` | Tools: the registry, dispatch with the permission gate, and `fetch_url`. Separate from the loop because a registry needs no loop; MCP and native toolsets register here later. |
| `embedstore/` | **Retrieval, with no model in the loop.** A SQLite chunk store over an *external-content* FTS5 index — the text lives once in `chunks`, and triggers keep the index in step on every write (`store.h/.cpp`). `fts.h/.cpp` quotes every query term and joins them with **OR** (implicit AND matches nothing for a real question), and maps BM25's unbounded negative score onto a comparable 0..1. `chunk.h/.cpp` splits on codepoint boundaries; `ingest.h/.cpp` walks a directory, skips binaries *by name*, and shells out to `pdftotext` when it is there. Every hit carries the name of the retriever that scored it. `vector.h/.cpp` adds float32 blobs, cosine, and **RRF fusion by rank alone**; the store holds a vector beside each chunk (`dim = 0` for lexical-only rows) and records the model that built them. Since Milestone Y (schema v3) a nullable `metadata` column rides beside the text — structured data a reader decodes afterwards, never indexed — with `update_metadata` rewriting it alone, `chunks_with_metadata`, `chunk_by_id`, `chunk_vector`, and every hit carrying its chunk's metadata. Since Milestone Y's third slice the store also holds the **knowledge graph** (schema v4): `graph.h/.cpp` -- the `kg_*` tables and the entity FTS index created on open, node identity by (normalised name, type), first-non-empty descriptions with the vector cleared on a text change, `upsert_edge` corroborating versus `ensure_edge` a fact at weight 1, mention dedup, the per-source fingerprint (count, highest chunk id, model), `reconcile_graph` pruning what chunk churn orphaned in one transaction, `delete_graph`, `graph_meta`; `graph_search.h/.cpp` -- stats, exact and full-text entity lookup, neighbourhoods, supporting chunks, and `graph_expand`, the pure-SQL walk (hops clamped to 1..2, ranked by connecting weight times mentions, capped, chunk seeds never re-listed). Chunk ids are AUTOINCREMENT since v4, because the fingerprint reads the highest id to catch a same-count re-ingest and a reused id would miss exactly that. Since the fourth slice (schema v5) the multi-collection shape is in use -- `add_mention`/`set_source_state`/`source_states` by member label, `reconcile_graph_multi` over `MemberStores` (ex-members' rows die, dead mentions pruned per member through the cross-database `chunk_ids_existing`, a null member reading as empty), `graph_stats_multi` with per-member coverage, `node_mention_refs`, `graph_expand_labelled`, the named graph's identity in `graph_meta` -- and the global layer: `graph_communities.h/.cpp` (`kg_communities`/`kg_community_members`, the summary written as a pseudo-chunk under `graph://community/<id>` in one transaction, its vector arriving later, prune by key, `communities_without_vectors` so a failed embed heals on the next run; pseudo-chunks excluded from planning, coverage and staleness and taken by `delete_graph`) and `graph_dedupe.h/.cpp` (`dedupe_nodes`: union-find per type over cosine, the earliest node surviving, edges repointed or folded with weights summed, self-loops dropped, mentions unioned and recounted, the description merged with the vector cleared, memberships removed; decision nodes and unvectorised nodes never considered; one transaction; a dry run writing nothing). `store_impl.h` is package-private: the connection and statement helpers the five translation units share, so the raw handle still never leaves the package. |
| `events/` | **The lifecycle bus** (Milestone U). A leaf package — it includes nothing from the project, and `harness.layering` holds it to that — so the session store, a backend loading a model, the job registry, anything, can publish without an include cycle. Bounded per-subscriber queues, non-blocking publish, drop-on-slow: delivery is advisory, never control flow. `bus.h/.cpp` |
| `secrets/` | **The provider credential store and the one key resolver** (Milestone U). `store.h/.cpp` — a `0600` `credentials.json` beside the config, one slot per API-billing provider type (`anthropic`, `openai`, `google`; vendor-CLI types have none), written through `write_file_atomically(…, private_mode)`; the type that holds a key is private to the `.cpp`, what the header hands out is `CredentialMetadata` with no key field, and `key_for` is the one function that returns one. `resolve.h/.cpp` — `resolve_api_key`: the entry's `api_key` > the stored slot > the conventional variable (`ANTHROPIC_API_KEY`; `OPENAI_API_KEY`; `GEMINI_API_KEY` then `GOOGLE_API_KEY`) from an `EnvSnapshot` captured once, reporting which rung answered; `takes_api_key`, `slot_type`, `no_key_message`. Sits beside the harness — it may include `harness/` and itself, and `harness.layering` holds it to that — and `cli.one_key_resolver` keeps it the only chain. |
| `tools/` | **The native toolsets** (Milestone V). What Ommi ran as five Python MCP servers, in `apogee_core`: `fs.h/.cpp` (read, write, delete, list, search inside a root — `resolve_in_root` follows symlinks and requires the root to be an ancestor **by path components**, the fix for Ommi's `startswith` sandbox), `shell.h/.cpp` (`run_command` through `/bin/sh -c` with the directory and command as positional parameters, never spliced into the script, a timeout that is a result, and — a divergence — `writes`, so it is gated), `git.h/.cpp` (status, log, diff, show as `git` children with list arguments; `valid_ref` closes option injection; `git_diff`'s review form `base...head` with fetch-on-demand, its defaults `ReviewDefaults` set by flags), `notes.h/.cpp` (a scratchpad under `notes/`, keys that can never be paths), `rag_query.h/.cpp` (`search_documents`/`list_collections`/`collection_info` over the store, resolved through `resolve_turn_retriever`, every hit naming its retriever), `process.h/.cpp` (run a child to completion, bounded), `args.h/.cpp` (tolerant argument access: `null` is "not supplied"), and `toolsets.h/.cpp` (`register_native_toolsets`, `destructive_tool_names`, `toolset_names`). Includes `agent/`, `agentloop/`, `embedstore/`, `platform/`, `harness/`; never `backends/` or `commands/` — `harness.layering` holds it to that. |
| `httpserver/` | **The `apogee serve` inference plane** (Milestone T) **and, since Milestone U, the control plane**: the admin token and bearer gate, the job registry, the lifecycle event stream, the config CRUD slice, and since Milestone Y the knowledge slice on the inference plane's own harness. Transport-neutral `HttpRequest`/`HttpResponse` types, a listener-free route table (`mux`) and handlers over the **same** agent loop every other surface runs, the third `Reporter` adapter (`sse_reporter` — chunks and opt-in meta-frames), a session store that persists ordinary chat sessions, and `serve.cpp`: the one file in the whole link allowed to reference `listen()`/`accept()`. Server deployments only. `admin_graph.h/.cpp` (Milestone Y): the graph twins -- the build as the plane's first **async job** on `JobWorkers` (cancel between chunks; the enabled write on success), stats never an error, entity lookup exact then fuzzy, delete, and the `PUT …/embeddings/{id}/graph` twin of the auto-enable write. Since the fourth slice every graph route resolves `{id}` graphs-first (a named build as a job over the members opened through `commands::open_graph_members`, stats with `members[]`, an entity's chunks naming their collection, delete removing the file), plus `POST …/communities` (the second job kind, `graph-communities`), `GET …/communities`, `POST …/dedupe` (synchronous), and `admin_graphs.h/.cpp`: the `graphs:` CRUD slice (`GET/POST /v1/admin/graphs`, `GET/PUT/DELETE …/{id}`, `built` on every response, the CLI's rules through `commands::validate_named_graph`, `409` on a POST collision). Since Milestone Z the datasets slice (`admin_datasets.h/.cpp`): list/create/get/delete over the same store the CLI writes, the kits listing, and synth as the third job kind (`datasets-synth`) on the plane's harness; and the training slice (`admin_training.h/.cpp`), **reads only** -- `GET /v1/admin/training/status|runs|runs/{id}|versions|cycle` off the manifests, the ledgers and the cycle history the CLI writes: `status` with the active pipeline, the lock state and the history's headline, `/runs` listing runs and pipeline runs tagged by kind (`?kind=` keeps one), `/runs/{id}` answering either, `/cycle` the history plus `active`; every control action -- `train run|eval|promote|rollback|setup`, `pipeline run|resume`, `regime run`, `cycle run|halt|resume` -- is a parity carve-out with no route, held by a test that sends every mutating method to every training path. Fifty-nine admin rows. |
| `mcp/` | **The MCP stdio client and the in-binary server** (Milestone W). `types.h/.cpp` — JSON-RPC framing hand-rolled (four methods; a dependency would be for nothing), the MCP payloads, `mcp__<server>__<tool>` namespacing split on the **first** `__`, `readOnlyHint` read from `annotations`. `transport.h/.cpp` — `Transport` as an interface so the test fleet is scripted transports and a remote transport would be one more implementation; `StdioTransport` over `platform::ChildProcess` with the JSONL framer, and a **mandatory** stderr sink plus the eight-line `StderrTail`. `client.h/.cpp` — the handshake, tools cached once, calls demultiplexed by id by a read loop whose error path calls `mark_done` (once, a compare-and-swap) so a dead server releases every waiter now. `registry.h/.cpp` — every configured server connected at startup, `[mcp] connecting:` before each dial, a bound covering connect *and* handshake, a bad server logged and skipped, tools registered as `writes` unless read-only, `mcp.server.*` on the bus. `serve_stdio.h/.cpp` — the other side of the same four methods over a process's own stdio, and `read_only_tools`/`dispatch_through` for `apogee __mcp-tools`. Guarded by `harness.layering` with one named allowance, the framer. |
| `scaffold/` | **The TTY-free resource cores** (Milestone W): `mcp_server.h/.cpp` — `create_mcp_server` writes a runnable Python server, its test and README from templates compiled into the binary (nothing seeded at install), or registers an existing executable, then appends the entry through the config editor; the one function `apogee mcp create` and `POST /v1/admin/mcp-servers` both call. `agent.h/.cpp` (Milestone X) — `create_agent` writes `prompts/<name>.txt` and `schemas/<name>-output.json` (starter texts or the bodies given) and appends the entry by relative path, `save_subdir` defaulting to the name; `agent_files` resolves them for `edit` and `delete --purge`; the one function `apogee agents create` and `POST /v1/admin/agents` both call. |
| `render/` | **The in-process report renderer** (Milestone X): `json_report.h/.cpp` — a schema-conforming JSON report to Markdown or plain text, top-level keys alphabetised with the reserved `human_summary` always last, arrays of objects as bullets keyed by their longest string, unparseable input returned unchanged. No second model call, ever. Includes only nlohmann/json. |
| `knowledge/` | **The organizational knowledge layer** (Milestone Y). `record.h/.cpp` — the canonical record (Ommi's full schema), `normalize`/`validate`, `index_text` (the immutable reasoning alone: never attribution, never the mutable link or status), `anonymize` (names off, chain on), `new_id`/`timestamp_for`, the JSON shape, and `record_from_metadata`, the one decoder of a chunk's record — strict, keyed by its own id. `clerk.h/.cpp` — the capture clerk: Ommi's prompt and schema compiled in (byte-identical to `assets/clerks/`, a test enforcing it), `with_output_format` over the same `schema_instruction` every agent ends with, `ClerkFn` as the injected model call, `run_capture` (overrides field by field, then normalise and validate; a non-conforming outcome a failed capture), `finalize`, and `make_structured_clerk` binding a harness and a model to `run_structured` at 0.2, 2048 tokens, no tools, a throwaway history, a side request. `store.h/.cpp` — one record is one chunk keyed by the record id (its text the index, its metadata the record), the raw conversation archived privately first and referenced by `raw_ref`, `put` replace-by-source so re-capture is idempotent, `list` newest first, `set_link`/`set_status`/`supersede` as metadata rewrites that leave the vector alone, `remove` taking the archive; since the second slice also `reindex` (every record or the named ones, an unknown id refused before any embedding, the archive untouched) and `vectorless_count`. `query.h/.cpp` — `query` through the ONE resolver over the collection's pins and the probed embedder: rank everything, `filter_hits` (records only, on the wanted branch and discipline), then cut, with the judge under its never-fail contract and `reranked` set from the ordering; `ScoredRecord` carrying the chunk id as a storage handle. `refine.h/.cpp` — the revision pass: Ommi's prompt compiled in, the same OUTPUT FORMAT block and schema as capture, `refine_user_message` (the six clerk-owned fields, the instruction, the raw or an explicit absence), `validate_refine_instruction` (2000 codepoints, before any clerk call), `run_refine` carrying `supersedes` and a dropped source through. `export.h/.cpp` — `export_records` (names off, chain on: attribution and the local `raw_ref` stripped) and `render_markdown` grouped by status. Includes `embedstore/`, `agentloop/`, `agent/`, `harness/`, `platform/`; never `backends/`, `commands/` or `httpserver/` — `harness.layering` holds it to that. |
| `graph/` | **The knowledge-graph layer over collections** (Milestone Y, third slice). `extract.h/.cpp` -- the extraction contract: Ommi's prompt and schema compiled in (byte-matched to `assets/clerks/extract_*`), the closed type set (`decision` never in it), the per-chunk caps, `normalize` enforcing in host code what the schema can only request (types, caps, self-loops, endpoints, duplicates), and `make_structured_extractor`, one `run_structured` call at temperature 0.2 under a 2048-token cap as a side request. `build.h/.cpp` -- the resumable build: reconcile → materialise every knowledge record as a `decision` node (name = record id, description = decision — intent clipped to 400 codepoints, status/discipline as metadata replaced every run, `concerns` edges to what the extractor pulls from its text, `supersedes` when the target is in the graph) → plan by the (count, max id, model) fingerprint → extract stale sources with one retry, a state row after a complete file only → embed mutated nodes last (a decision by description alone), a failed embed never losing extraction. Model-free: generation and embedding arrive as `ExtractFn` / `EmbedFn`. Since the fourth slice `build_multi` runs the same loop over `Member`s (a collection label and a read-only store view; null for a missing member) into a target store with labelled mentions and per-member state so a resume works across members, `build` being the single-member `""` case; `BuildOptions::graph_name` stamps a named graph's identity. `communities.h/.cpp` -- the global layer: Ommi's summariser prompt compiled in (byte-matched to `assets/clerks/community_prompt.txt`), `detect_communities` (deterministic weighted label propagation: labels from node ids, ascending-id asynchronous updates, weighted-majority adoption with the smallest label winning ties, at most 20 rounds, edgeless nodes never joining, a min size of 3), `community_key` (the sorted member ids verbatim -- the exact-staleness identity), `community_text` (members most-mentioned first, at most 20 relations by weight), `make_summarizer` (one plain generation call, no schema, a side request) and `build_communities` (unchanged keys skipped, new or forced ones summarised, the rest pruned, then every summary still without a vector embedded last; a summariser failure soft). Includes `embedstore/`, `knowledge/`, `agentloop/`, `harness/`, `platform/`; never `backends/` or a surface -- `harness.layering` holds it to that. |
| `training/` | **The training track's domain core** (Milestone Z). `script_runner.h/.cpp` -- the C++ side of the Python boundary: a driver under the environment's interpreter, stdout framed by `backends/jsonl_framer.h` (a named layering allowance, like `mcp/`'s), every line classified (`{"message"}`, `{"error"}` as its own event, a terminal record, a non-JSON line kept as a message), the exit code carried, stderr a bounded tail never inherited, cancellation terminating the child; `run_command` for venv and pip. `python_env.h/.cpp` -- `PythonEnv` over `training/venv/`: created from a base interpreter with `-m venv`, requirement sets (`prepare`, `mlx`, `peft`, `convert`; version floors) installed with its own pip and recorded in `apogee.json`; `locate_base_python` from `training.python` or PATH; an injectable `CommandRunner`, so no test runs Python. `kit.h/.cpp` -- Ommi's kit shape parsed with yaml-cpp, validated, listed sorted with a broken file named, the inline eval suite as JSONL. `synth.h/.cpp` -- the model-free distillation core: the contract prompt, seed cycling, `extract_json_array`, the field aliases, prompt de-duplication, the call bound, parallel batches with retries and backoff, a `GenerateFn` closure for the teacher. `datasets.h/.cpp` -- the dataset store (one writer, temp file and rename, `role` before `content`, shapes), the template lines, and the session miner over plain `SessionView`s so the package never reads the session store. **The run** (the second item): `trainer.h/.cpp` -- the `Trainer` and `CandidateRunner` interfaces, `TrainRequest`, the `ProgressEvent` protocol with `parse_progress_line` (an `{"error"}` an Error, a non-JSON line a Message, a record a Message carrying its line), `select_trainer` over a `HostShape`, `validate_student`; `script_trainer.h/.cpp` -- one class for both drivers over the script runner with the argv contract exposed (`train_arguments`/`fuse_arguments`/`infer_arguments`) and `PYTHONDONTWRITEBYTECODE` on every run so the seeded tree stays byte-identical; `mlx_trainer.h/.cpp` and `peft_trainer.h/.cpp` -- the two specs; `mock_trainer.h/.cpp` -- scripted iterations, a token adapter, a copying fuse, an echoing candidate (the base prefixed), and `write_mock_gguf`, a minimal GGUF the header reader parses; scripted by the dataset's first line (`{"mock": {"error", "fuse_error", "iters"}}`, the fuse failure carried in the adapter it writes) the way the mock backend is scripted by its file, so a failed run and a failed promote are driven through the real command line; `manifest.h/.cpp` -- `RunManifest` round-tripping JSON, written atomically at start and end, `new_run_id` with the collision suffix, `valid_run_id`, `dataset_digest` through `models/sha256.h`; `store.h/.cpp` -- the `VersionLedger` (entries kept as history when pruned) and `TrainingStore`, the read side every surface and the HTTP routes share; `eval.h/.cpp` -- `run_eval`: contains, pairwise judge through a `JudgeFn` closure with the first-word verdict, skip, the never-fail contract, the 100% gate; `promote.h/.cpp` -- `eval_gate`, `plan_promotion` with `next_version` as `max + 1`, `build_promotion_artifacts` over the `Converter`/`Verifier`/`Quantizer` closures removing what a failed step left, `record_promotion` with `prune_candidates` never naming the active version, `rollback_target` (`GateMode` lives in `eval.h`, read by promote and the pipeline alike). **The orchestration** (the third item): `pipeline.h/.cpp` -- `PipelineRunManifest` (a stage record per stage, `last_passed`, `first_unpassed`, `promote_run_id`) round-tripping JSON and written atomically on every transition, `run_pipeline`/`resume_pipeline` over `ResolvedStage`s (the caller resolves every name; the core never does) driving the trainer, `run_eval` on `merge_suites` and the fuse for every stage but the last, `resume_check`, `mix_datasets` deterministic from the sizes alone, `stage_run_id`; `regime.h/.cpp` -- `apply_regime_flags` (flags win; `--all-kits` alphabetical unless `--kit` says otherwise), `validate_regime_kits`, `regime_stage` (the kit's `train:` block as defaults), `run_regime` synthesising through the synth core into the work directory with each kit's eval materialised and running the pipeline `<id>-pipe`; `cycle.h/.cpp` -- `CycleHistory` atomic under `cycle/history.json`, `CycleLock` created exclusively through the platform seam (`platform::create_exclusive_file`, one atomic create-or-fail carrying the pid) and removed on release, `check_circuit_breaker`/`halt_cycle`/`resume_cycle`/`trip_breaker`, `collect_directory`/`consume_directory_files`, `collect_sessions` (refused without consent naming the two risks; the watermark and the `datasets create` filters through the one miner), `merge_datasets`, `anchor_gate_pass`/`last_passing_cycle_score`/`record_cycle_run`, and `run_cycle` -- the lock, the breaker, the sources, the merge, the pipeline with every stage's dataset replaced, the dual gate, then the `PromoteFn` closure and the consume, the history saved at every outcome. `store.h/.cpp` also lists pipelines (`list_pipelines`, `get_pipeline`, `active_pipeline`). Includes `harness/`, `platform/`, `agentloop/`, `agent/`, the framer and `models/sha256.h` by name, and itself -- `harness.layering` holds it to that. |

A reserved package carries a documented header and no code. It exists so a later backlog item has one obvious home, and `tests/packages_test.cpp` includes each one so a broken include path is caught the day it breaks rather than months later. `embedstore/` graduated out of that set in Milestone Q and `httpserver/` in Milestone T; each umbrella header now names the package's real headers, so the same test keeps checking the same thing.

### `commands/` — the subcommand scaffold

| File | Purpose |
|---|---|
| `command.h` | The `Command` interface (`name`, `summary`, `bind`) and `RootContext`, the root-level state every command can read. |
| `registry.h/.cpp` | `CommandRegistry` — owns commands, rejects duplicate names, binds them all to an app. `default_registry()` is the built-in set. |
| `root.h/.cpp` | `RootCommand` — persistent flags (`--config`), the version flag, and `run(argc, argv)` → exit code. |
| `version_command.h/.cpp` | `apogee version`. The first real subcommand, proving the path end to end. |
| `complete.h/.cpp` | `apogee complete` — one-shot prompt in, answer out. The walking skeleton, and the first consumer of the agent loop (`--tools`). |
| `ask_prompt.h/.cpp` | The terminal `ask_user` implementation. Prompts through the status line, reads stdin, and returns a **null** AskFn when there is no terminal — which is what stops the tool being advertised. |
| `terminal.h/.cpp` | `TerminalWriter` — the single mutex every terminal write goes through. |
| `status_line.h/.cpp` | The self-overwriting status line and the spinner, with the generation counter that invalidates an in-flight repaint. |
| `thinking_view.h/.cpp` | The rolling reasoning window and its collapse-to-summary. |
| `cli_reporter.h/.cpp` | The terminal `agentloop::Reporter` adapter. One implementation, shared by every interactive surface. |
| `json_reporter.h/.cpp` | The **machine-mode** `Reporter` adapter: the same loop events as one JSON object per line. Sibling of `cli_reporter` over one seam, which is why a capability reaches both surfaces at once. Also owns `OutputFormat`/`InputFormat`, `parse_driver_line()` for stdin, and `make_driver_ask_fn()` — `ask_user` over the protocol, offered only to a driver that reads structured input. Documented for front-end authors in [machine-mode.md](../reference/machine-mode.md). |
| `chat.h/.cpp` | `apogee chat` — the REPL, slash dispatch, and context monitoring. Since Milestone X also `--branch`/`--base`/`--remote`/`--fetch|--no-fetch` and `/branch`: the review context as the git tools' live defaults (re-pointed without rebuilding the registry) and a system note that rides the transient prefix, never the transcript. Since Milestone Y also `/capture [status|link]` — the live conversation through the capture core with the loaded model as the clerk — and `knowledge.auto_capture` on a clean exit only. |
| `serve_cmd.h/.cpp` | `apogee serve` — `--print-admin-token`; mounts the control plane on the same listener with the startup config as its drift snapshot; flags to `httpserver::HandlerOptions` and `ServeOptions`, the served-backend set (the default or `-m` alone unless `--all-backends`; vendor-CLI entries skipped by type and announced), `--preload` through `Harness::preload_model`, `--ignore-timeout` as an in-memory config override, and the bind refusal before anything listens. Server deployments only. |
| `chat_history.h/.cpp` | `apogee chats` — list, info, title, delete — and the auto-title prompt. |
| `input_gate.h/.cpp` | The startup typeahead flush. Called once, immediately before the first prompt. |
| `line_reader.h/.cpp` | `LineReader` and its two implementations — `PlainLineReader` (getline, for a pipe) and `EditingLineReader` (replxx, for a terminal). Since Milestone Y `interrupted()`: whether the last `read` ended on Ctrl-C (replxx sets `EAGAIN`) rather than on the end of input — what lets `auto_capture` skip an abandoned session. |
| `helpers.h/.cpp` | Shared command plumbing: flag/config resolution, stdin, base64, image parts, message assembly, and the exit codes. Also `choose_rag_collection` and `describe_retrieval` — **one decision for which collection a turn retrieves from**, called by `complete` and `chat` alike so the precedence (flag > `auto_rag`; `--rag ""` is the off switch, distinct from no flag) cannot differ between surfaces, and one sentence for the status line that always names the chunk count, top score, retriever, and whether the config rather than a flag asked for it. `retrieve_for_collection` gathers the collection's pins, the embedder, and the judge for `agentloop::retrieve_for_turn`, so `complete`, `chat`, and `serve` cannot assemble the facts differently. `configured_backend_key`/`names_a_configured_backend` map an explicit model to the entry it names without the router's fallback to the default (a typo must never answer from another backend), and `make_built_in_tools` is the one `--tools` registry all three surfaces build. `make_built_in_tools(BuiltInToolOptions)` — `fetch_url` plus `tools::register_native_toolsets`, honouring `tools.disabled` — is the one place that decides which tools a `--tools` run has — since Milestone W it also connects every enabled `mcp_servers:` entry on the caller-owned `mcp::Registry` and registers their tools last, with `mcp_status_line` repainting progress on the status line and keeping warnings. Since Milestone X `BuiltInToolOptions` also carries the agent's policy and server list: `apply_tool_policy` runs over the finished registry last (`read-only` keeps only tools with no `writes`, MCP included; `none` builds nothing and dials no server), `mcp_servers` selects which entries connect (null means every enabled one; a name matching no entry is reported and skipped), and `live_review` is the shared value the git tools read at call time. |
| `config_cmd.h/.cpp` | `apogee config` and its eleven subcommands (`set-permission` since Milestone V, `delete-mcp-server` since Milestone W). A thin caller — it formats no YAML of its own; every mutation goes through `harness/config_edit.h`. `get` resolves dotted keys including `auto_rag`, `embeddings`, and `embeddings.<name>[.<field>]`. |
| `auth_cmd.h/.cpp` | `apogee auth` — `add`, `list`, `clear`, each taking a provider type. `add` takes the key through a hidden prompt (`platform::read_hidden_line`, echo off), `--stdin`, or `--from-env` (copying the conventional variable into the store) — **never on the command line**, which lands in shell history and process listings. `list` renders metadata and, per configured backend, which rung answers. A vendor-CLI type is refused with the principle; an unknown name with the accepted list. |
| `permissions.h/.cpp` | The permission gate's two halves as every surface wires them: `make_permission_checker` (the config's level for the tool, then the session's `session` answers, then `Ask`), `terminal_confirm_fn` (`[y]es / [n]o / [a]lways / [s]ession` through the status line; `always` written as `permissions.<tool>: allow` through the config editor; null when there is no terminal, which is what makes `ask` deny on a pipe), and `make_driver_confirm_fn` (machine mode's `question` with `"kind":"permission"`, answered by one `answer` line). `ToolGate` carries both to the loop. |
| `mcp_cmd.h/.cpp` | `apogee mcp create` (scaffold or `--command` register-only, through the scaffold core), `list` (connects to each and shows live state and the negotiated protocol), `test <server> <tool> [json]` (the named server force-enabled, one call through the production client), `enable`/`disable` (one line of the config). And the hidden `__mcp-tools`: the read-only native toolsets served over this process's stdio — built from the toolsets directly, never the MCP registry, so a config listing this very command cannot spawn itself without end. |
| `analyze.h/.cpp` | `apogee analyze` (Milestone X): a named or ad-hoc agent run one-shot, `--interactive`, or in machine mode over the same loop; the persona is the agent's prompts plus the OUTPUT FORMAT block plus the review note, never the chat persona; input precedence positional > `--input` > piped stdin; the registry filtered by the agent's policy; `run_structured` when a schema is in play; the rendering rules (`resolve_rendering`), the quiet-save rule (`should_print_report`), and `report_filename` (`<base>-YYYYMMDD-HHMMSS.<ext>`, no colon). Vendor-CLI backends refused by type before anything is built. |
| `agents_cmd.h/.cpp` | `apogee agents create|list|edit|delete` (Milestone X): `create` through the scaffold core, prompting for the description, model and policy on a terminal and taking the defaults on a pipe; `list` with the bundled three and every entry; `edit` in `$EDITOR` through `platform::run_foreground`; `delete` removing the entry and, with `--purge` or a yes at the terminal, the files. |
| `knowledge_core.h/.cpp` | **The capture core every surface calls** (Milestone Y): `knowledge_collection` (`--db` > `knowledge.db` > `knowledge`), `decide_store` (the collection's pin, the embedder through the capability probe, `resolve_ingest_retriever` under the spend rule), `draft_capture` (the clerk, then an id-less draft — what a dry run prints), `store_record` (mint what is missing and keep what is given, embed per the decision, archive, write, flip a superseded record, register the collection through `append_embedding` — reported, never fatal), and `capture_and_store`, which refuses a resolver's refusal **before** the clerk runs. The CLI, chat's `/capture`, the exit-time auto-capture and the HTTP twin all go through it, which is what makes their records the same record. |
| `knowledge.h/.cpp` | `apogee knowledge capture` (alias `kn`, Milestone Y): the raw conversation from the argument, `--input <file>`, `--from-chat <id>` (rendered through `logger::transcript_text`, `source: chat` unless overridden) or piped stdin; the clerk's backend through the extraction role with `-m` winning and a vendor CLI refused by type before anything is built; `--status` normalised through the same synonyms the clerk's answer gets; `--dry-run` printing the draft and `Would store in "<db>" (retriever: <r>)` with zero footprint; `--json` as `{draft, record, db, retriever[, warning][, note][, notes], registered}`; a clerk that never conforms exits 2 and stores nothing. Since the second slice also `query` (shipped by default, `--status ""` for every branch, the retriever named on every result, providers built only when a decision could need an embedder or a judge), `list`, `info [--raw]`, `link`, `status` (synonyms folded), `delete`, `export [--format json|markdown] [--anonymize] [--out]`, and `reindex [ID] [-m]` (honest about a collection with no records or no vectors before any backend is built, a note on a mixed one, the binding recorded after). A read never creates a collection, and a config that will not load costs it only a warning. `query --graph` walks the collection's graph from the matched records through `knowledge::graph_for_records` -- the same core the HTTP `?graph=true` calls -- printing a `Related (knowledge graph, N entities):` section, or the note that says how to build one -- through `resolve_turn_graph`, so a built named graph covering the collection is what the walk uses. |

**Adding a subcommand** touches exactly two places: the command's own `.h`/`.cpp` pair, and one `registry.add(...)` line in `default_registry()`. It then appears in `apogee --help` with no other edit — `main.cpp` never grows.

A command signals failure by throwing `CLI::RuntimeError(code)`; `RootCommand::run` turns that into the process exit code. Read `RootContext` inside your callback, never at bind time — parsing has not happened yet at bind time.

### The platform seam

`platform/` exposes the build's OS, architecture, and release-target name. The target name is one of the exact six strings shared by `cicd.sh --platform`, the CMake presets, the CI matrix, and the binary itself; `tests/platform/platform_test.cpp` is what keeps them one vocabulary.

Mechanisms that land here as later items need them: process spawning (vendor-CLI backends), PTY and terminal control (the chat UX layer), file-mode enforcement (the `0600` secrets rule), socket peer checks (the admin plane), and `read_hidden_line` — a prompt with echo off (termios on POSIX, `SetConsoleMode` on Windows, plain `getline` on a pipe), restored on every path, for `auth add`. Each arrives as a declaration in `platform.h` with one definition per platform. **Where a mechanism has no Windows equivalent, the owning item records the skip** rather than leaving the gap silent.

---

### `harness/` — the config engine

| File | Purpose |
|---|---|
| `paths.h/.cpp` | Apogee's on-disk layout, rooted at `~/.apogee` and relocatable with `APOGEE_HOME`. `default_config_path()` is what a command uses when `--config` is absent. Since Milestone X also `home_for_config`: the data directory a config path lives in, which the scaffold cores and the agent loader derive paths from so a `--config` temp tree stays hermetic. |
| `config.h/.cpp` | The **read path**: `Config`/`BackendConfig` structs, `parse_config`, `load_config`, `expand_env`, and the backend-type table. The only code that uses yaml-cpp. `BackendConfig::embedding_model` names the model an entry uses when it **embeds** rather than chats; empty means the vendor's default, decided by the provider rather than here, so the loader knows no vendor. A collection entry also carries `backend:` (which entry embeds it), `retriever:` (a pin, validated by `check` rather than the loader), and `rerank:`. Since Milestone X also `agents:` (`AgentConfig`, `AgentToolPolicy`, `AgentOutputFormat`, every enum validated at load) and `is_vendor_cli`, the one predicate `serve` and `analyze` both refuse by. Since Milestone Y also `knowledge:` (`KnowledgeConfig`: `auto_capture`, a boolean; `db`, a plain collection name refused at load otherwise; `collection()` for the effective name), and `graphs:` (`NamedGraphConfig`: `collections`, `extract_backend`, `hops` validated 1..2, `max_entities`; keyed by name and kept in file order because the precedence rule reads them first to last; `find_graph`, `graph_names`). |
| `config_template.cpp` | The starter config, embedded as a raw string literal **generated from `assets/config.yaml`**. A test holds the two byte-identical. |
| `config_edit.h/.cpp` | The **write path**: pure text-surgery transforms plus `edit_config_file`, the only thing in Apogee that writes a config. The entry helpers — `append_backend`/`delete_backend` and `append_embedding`/`delete_embedding` — are thin callers over two section-generic internals, `append_entry` and `delete_entry`, so the placement rule (above trailing comments), the separator rule, the collision rule, and comment ownership on delete exist once. Since Milestone X also `append_agent` (a string or list only when set, a boolean only when true, `tools` always) and `delete_agent`. `set_embedding_graph_enabled` (Milestone Y) sets `embeddings.<name>.graph.enabled` in place or appends the block, scoped to `embeddings:` so a same-named backend is never touched -- the one write the first graph build makes, CLI or HTTP. `append_graph`/`delete_graph` (the fourth slice) are thin callers over the same section-generic internals -- `collections` as a flow list, `extract_backend` when set, `hops`/`max_entities` when not the default -- so a graph sharing an agent's or a server's name can never touch the other section's entry. |

**The rule that governs this package** is in [CLAUDE.md](CLAUDE.md) → *⚠ One config mutation path*: every config change goes through `config_edit.h`, and nothing ever serializes a `Config` back to disk. Reading is `load_config`; writing is text surgery. They are separate paths on purpose.

**Why text surgery.** yaml-cpp — like every mainstream YAML library — discards comments on parse and reorders keys on emit. A config file's comments are most of its documentation, so load-modify-save would silently delete the user's content. The helpers instead locate the affected lines and splice them, leaving every other byte alone: CRLF stays CRLF, a missing final newline stays missing, and a comment block documenting the *next* entry survives deleting this one.

**Adding an edit helper.** Write it as a pure `std::string(std::string_view, …)` transform in `config_edit.cpp`, next to its siblings — and if it adds or removes a map entry under some section, write a renderer for the entry and call `append_entry`/`delete_entry` rather than re-deriving where an entry goes. Give it a golden-file test in `tests/harness/config_edit_test.cpp` that asserts a byte-identical round trip against the comment-dense fixture, and route the command layer through `edit_config_file` so it inherits re-parse validation and the atomic write for free. Do not add a second function that opens the file itself.

**Widening the backend type set.** Add a row to `kBackendTypeNames` in `config.cpp` and an enumerator in `config.h`. The loader dispatches through that table, so nothing else changes — and `every backend type round-trips through its name` fails if the two ever disagree. Each widening is a recorded decision in the item that makes it, never a silent edit.

### `harness/` — the provider interface, IR, and router

| File | Purpose |
|---|---|
| `types.h/.cpp` | The canonical IR: `ChatMessage`, dual-mode `MessageContent`, `Tool`/`ToolCall`/`ToolResult`, `ChatRequest`/`ChatResponse`, `Usage`, `ModelInfo`, `RAGMeta`, `StatusEvent`, and their JSON conversions. |
| `provider.h/.cpp` | `LLMProvider` (four methods), `TokenSink`/`StreamOptions`, and the optional capability interfaces. |
| `harness.h/.cpp` | `Harness` (registry + capability probes) and `SimpleRouter` (the three-rung precedence). |
| `behavior.h/.cpp` | `ModelBehavior` — plain data, the layering seam. |
| `cancellation.h/.cpp` | `CancellationToken` — a shared atomic flag, cancellable from any thread. |
| `context_windows.h/.cpp` | The compiled model → window fallback table. Single owner. |
| `errors.h/.cpp` | Typed errors: `NoAvailableBackendError`, `ProviderNotRegisteredError`, `ProviderError`, `CancelledError`, `InvalidRequestError`. |

**Two rules govern this package**, both in [CLAUDE.md](CLAUDE.md) → Invariants: *⚠ The harness never includes backends* and *⚠ Capability probes never leak a cast*.

**The IR never grows provider-specific fields.** When a provider needs something nothing else has, it belongs in that backend's config entry or its adapter. The moment one vendor's concept appears in `types.h`, every other backend has to decide what to do about it.

**The transient region.** `ChatRequest::Transient` carries per-turn state that must never reach the wire or a session file — a RAG span, the side-request flag, a response schema. It is a nested struct with **no `to_json` anywhere**, so serializing it does not compile. Use `durable_messages()` for the history view. If a RAG blob reaches persisted history it is re-sent on every later turn, growing the prompt without bound — silent, and expensive.

**Routing** resolves in three rungs, each trying the literal name then the normalized form (lowercased, `.`/`:` → `-`): exact backend key, then a backend entry's `model:` field, then `models.default`. A backend key beats another entry's model field, or `-m <key>` would be ambiguous.

**Adding a backend.** Implement `harness::LLMProvider` in `backends/`, translating to and from the IR. Inherit a capability interface only if you have that capability — the Harness discovers it. Add a row to `kBackendTypeNames` in `harness/config.cpp` for the config `type:`. Model the shape on `backends/mock.h`, and honour cancellation *between chunks*, not just at entry.

### `backends/` — provider implementations

| File | Purpose |
|---|---|
| `mock.h/.cpp` | `MockProvider` (scripted turns, configurable chunk size, recorded requests) and `MockEmbeddingProvider`. No network, no model. Since Milestone X a `type: mock` entry with a `model_path` answers from a JSON script of turns (`parse_mock_script`/`load_mock_script`), tool calls included, with `{{last_tool_result}}` / `{{system}}` and their `:json` forms expanded against the request (`expand_mock_text`) — how the real binary is driven through a tool-calling review with nothing installed. |
| `http_client.h/.cpp` | `HttpTransport` (one request; `CurlTransport` is the real one) and `HttpClient` (retry/backoff over a transport). Shared by every cloud backend. |
| `sse_parser.h/.cpp` | A byte-fed Server-Sent Events state machine. Shared by every streaming cloud backend. |
| `anthropic_wire.h/.cpp` | IR ↔ Anthropic Messages API translation, and the extended-thinking replay cache. Since Milestone X also structured output: `output_format` plus the `structured-outputs` beta header where `supports_native_structured_output(model)` says the field exists (the 4.5 generation and later, Opus 4.1; both id spellings parsed), else one forced `structured_output` tool whose `input_schema` is the answer's schema — `tool_choice` by name with no other tools, `any` with them, none under extended thinking — and `fold_structured_output`, which turns that call back into answer text in both the streamed and the non-streamed path. |
| `anthropic.h/.cpp` | The Anthropic provider: streaming and non-streaming chat, `list_models`, `count_tokens`. |
| `openai_wire.h/.cpp` | IR ↔ OpenAI **Responses API** translation. `input_items()` returns an array: one IR assistant turn with tool calls becomes a message item plus a top-level `function_call` item per call. Since Milestone X a `response_schema` becomes `text.format` (`json_schema`, `strict: false` — strict rejects a schema with optional fields), beside the tools. |
| `openai.h/.cpp` | The OpenAI provider: POST `/v1/responses` with a bearer token; maps `response.*` semantic events onto the sinks. Also implements `EmbeddingCapable`: `POST /v1/embeddings` with the entry's `embedding_model` (default `text-embedding-3-small`), batched at the documented 2048 inputs, same key handling and retry as chat, a bounded 120 s timeout because one body comes back. |
| `openai_embed.h/.cpp` | The embeddings wire translator for OpenAI, pure functions like the chat one. Rows are placed **by `index`**, never by position — the API does not promise request order, and a parser that trusts it hands input 0 the vector for input 1 with no symptom anyone can see. Refuses a body whose row count does not match the request. Fixtures are **shaped from the documented format, not recorded**: Apogee holds no key to record with. |
| `google_wire.h/.cpp` | IR ↔ Gemini `generateContent` translation. The assistant role is `model`; a tool result is a `functionResponse` part in a **user** turn, matched by name. Since Milestone X a `response_schema` becomes `responseMimeType: application/json` plus `responseSchema` reduced by `gemini_response_schema` to the OpenAPI subset the API accepts — and only on a request with no tools, because the API refuses JSON mode with function calling. |
| `google.h/.cpp` | The Gemini provider: model in the URL path, key in the `x-goog-api-key` header. Re-parses a whole response per chunk, since Gemini streams full objects rather than typed deltas. Also implements `EmbeddingCapable` through `:batchEmbedContents`, batched at 100 rows, the **embedding** model in the path rather than the chat model. |
| `google_embed.h/.cpp` | The Gemini translator. The model appears in the URL **and**, prefixed `models/`, inside every row — omit the prefix and the whole batch fails; rows come back in request order with no index, so order is trusted here where it is checked for OpenAI. |
| `embedding_batch.h/.cpp` | `batch_ranges`: the one piece every embedding client shares, splitting a list at a provider's maximum. Batch-first is a Core constraint — per-chunk calls were Ommi's recorded cost trap. A maximum of 0 is treated as 1 so the split always makes progress. |
| `llama_runtime.h` | The slice of llama.cpp the local backend needs, behind an interface — model load, tokenize, decode-at-position, sample, trim, capacity, batch limit. Plus `embed_batch` and `embedding_dimensions` on `LlamaModel`: mean-pooled, L2-normalised vectors from a **dedicated embedding context** with a unified KV cache — see Milestone R for the two live findings behind those two parameters. |
| `llama_real.cpp` | The real runtime over llama.cpp's C API, compiled only under `APOGEE_ENABLE_LLAMA`; otherwise the "not built in" answer. Wraps every raw handle in a `unique_ptr` with a custom deleter. |
| `chat_template.h/.cpp` | Prompt framing for local models: ChatML, Llama 3, Mistral, a narrow name-matching registry, and ChatML as the documented fallback. A GGUF's own template always wins over this. |
| `llamacpp_tokens.h/.cpp` | Exact prompt counting and `common_prefix_length` — the KV cache's entire decision. |
| `llamacpp.h/.cpp` | The local provider: model lifecycle, one live context per conversation, a throwaway context per side request, streaming, idle unload. Also `TokenCounting`, `VisionCapable`, and `StatusReporting`. Also implements `EmbeddingCapable` over `llamacpp_embed`; an embed counts as a use for the idle timer and runs the idle check on the way in. Since Milestone X a `response_schema` is stated in the system block as text (`messages_with_schema`), once — skipped when a system message already carries the OUTPUT FORMAT block — because the pinned subtree has no schema-to-grammar converter. |
| `llamacpp_embed.h/.cpp` | The provider side of in-process embeddings: texts to the runtime in slices of 64 so a Ctrl-C between slices is honoured promptly, and a count check on what comes back. Testable against the scripted runtime, which is the only place it is tested on the merge-blocking target. |
| `jsonl_framer.h/.cpp` | Bytes → complete JSONL lines, with a carry buffer across reads. Pure, no I/O — which is what makes the adversarial chunk-size replay possible. The vendor-CLI family's worst bug class lives here. |
| `cli_event.h` | The typed event union a vendor-CLI child produces. In `backends/` deliberately: the harness already has typed sinks, and a CLI-shaped union in `harness/` would put vendor knowledge in the one layer forbidden to have it. |
| `claude_cli_events.h/.cpp` | Wire JSON → typed events for the `claude` CLI's `stream-json` output. Unknown types and malformed lines are dropped, never fatal. |
| `claude_cli.h/.cpp` | The Claude-CLI provider: one child per session, resume-then-replay recovery, side requests on their own child, and `complete_structured()` over `--json-schema`. |
| `commands/helpers.h/.cpp` → `attachment_refusal` | **The one attachment guard**, called by every surface that accepts `--image`. It exists because the check was written inline in `complete.cpp` and simply never written in `chat.cpp`, so `apogee chat --image` handed pictures to a provider that had just said it could not read them. Returns a message rather than printing, so each surface delivers it in its own idiom — prose on a terminal, an `error` event in machine mode. |
| `harness/roles.h/.cpp` | **The one role-resolution chain**: `override > per-feature pin > role pointer > models.default`, whitespace trimmed at every rung. `resolve_backend()` also reports **which rung answered**, so a caller can explain a choice without re-walking the chain — a second walk is a second chain. Ommi shipped this logic twice and its CLI and HTTP plane disagreed; `cli.one_role_resolver` is what keeps there being one. |
| `models/acquire.h/.cpp` | The acquisition ladder: stream to `<name>.partial`, check size and digest **where the source provides them**, parse the header, and only then rename. A failure at any rung lands nothing — not the file, not the sidecar, not the partial. Source-agnostic (bytes arrive through a `ByteSource` callback), because Ommi's acquisition lived inside its one source and a second entry point bolted on is how the discipline gets implemented once carefully and once carelessly. |
| `models/sidecar.h/.cpp` | The record beside each acquired model. Keeps what the source **claimed** separate from what is **on disk**, from the first version — Ommi retrofitted that split after shipping a transform and left every older record ambiguous. `Verification` is a set of flags, not a boolean, so "no digest was published" (the common case with no allowlist) stays distinguishable from "the digest did not match". |
| `models/quantize.h/.cpp` | `llama_model_quantize` behind `APOGEE_ENABLE_LLAMA`, with an `#else` branch that **refuses by naming the flag**. The command exists in every build: a subcommand that is simply absent reads as "Apogee cannot do this", one that refuses reads as "this build cannot". Catches an already-quantized input from the header first — llama.cpp does refuse it, but only after a couple of hundred per-tensor log lines. |
| `models/sha256.h/.cpp` | Streaming SHA-256. Ours because nothing in the build hashed: libcurl exposes no portable digest API and OpenSSL is not a dependency, so adding one across six targets for a single function was the worse trade. Checked against independently produced vectors at every block boundary. |
| `models/source_ollama.h/.cpp` | The user's Ollama store: manifest → model layer → blob. Reads a **model store, not credentials**, which is why `cli.no_vendor_credentials` deliberately does not list `~/.ollama/`. **Read-only by construction** — its blobs are shared between models, so the module has no delete path to misuse. |
| `models/source_hf.h/.cpp` | Hugging Face, entirely new (Ommi had one source). A repo holding several GGUFs is **refused with all of them listed** rather than guessed at — a quantised upload holds a dozen precisions and picking one spends the user's bandwidth on a file they did not choose. No OAuth and no token store: `HF_TOKEN` is read at the point of use. |
| `commands/models_pull.h/.cpp` | The mutating verbs — `pull`, `delete`, `repair` — in their own translation unit so "what can this command destroy?" has a short answer. `plan_delete` refuses any name that resolves outside the models directory, twice over. |
| `commands/models.h/.cpp` | `apogee models list / info / status`. `list` renders a table or JSONL under `--output-format stream-json`. `ARCH` and `PROFILE` are separate columns on purpose: an architecture is what the file says it is, a profile is what Apogee knows about how that family behaves — a model can have a perfectly good architecture and still read `unprofiled`, which is the honest answer rather than a gap in the table. `info` also prints the profile's evidence line, so "how do you know?" has an answer at the surface. |
| `codex_cli_events.h/.cpp` | Wire→typed-event mapping for `codex exec --json`. Whole items rather than deltas: a long answer is one event. |
| `codex_cli.h/.cpp` | The Codex provider. **Pins the sandbox to `read-only`** — this CLI executes model-generated shell commands — and builds `exec` and `exec resume` argv separately, because the subcommand rejects flags `exec` accepts. |
| `model_profile.h/.cpp` | What Apogee knows about a **family** of local model — reasoning wrappers, control-token headers, tool dialect — and the ladder that resolves it: an explicit `chat_template:` > the GGUF's own `general.architecture` > a name hint > nothing. Rung 2 above rung 3 on purpose — an architecture is a fact in the file, a name is a string somebody chose. Every profile carries a `verified` flag and an **evidence line naming the model that was run**; only `gemma3`, `qwen3` and `gpt-oss` are verified, and a test asserts that list. **Headers have no permissive default and reasoning pairs do**, which is not an inconsistency: a header is deleted outright once matched, so guessing one for an uncharacterised family risks deleting its answer. A known profile's empty reasoning list means "emits none" (a result); nullptr means uncharacterised (permissive). |
| `think_filter.h/.cpp` | Separates a model's private reasoning from its answer, streaming, with a hold-back buffer sized to the longest marker so a tag split across two tokens is still caught. Built against a real leak: Qwen 3.6 answered "What is 2+2?" with `<think>\n\n</think>\n\n4` and every character reached the user. An empty pair list is a pass-through that does no arithmetic — Ommi's version panicked on a negative slice there. |
| `markup_filter.h/.cpp` | Removes control-token **headers** a model leaks into its own answer. Deliberately not the think filter: a reasoning wrapper *encloses* content and routes it somewhere, a header is a bare marker naming a section and carries nothing, and one file doing both would be wrong about both. Grammar: OPEN, an identifier run, optional whitespace, an **optional** CLOSE — binding to the identifier rather than the close is what keeps an unterminated header costing one word instead of a paragraph. Both gpt-oss shapes are handled: `<\|channel\|>NAME<\|message\|>` closes, `<\|start\|>NAME` does not. |
| `native_tool_calls.h/.cpp` | Tool calls a model emits as its **own tokens** rather than through an injected prose protocol. Unparsed, such a call is not a no-op: nothing dispatches AND the markup prints as the answer. Grammar, verified against gpt-oss-20b: `<\|channel\|>commentary to=functions.NAME <\|constrain\|>json<\|message\|>{json}`, the constrain hint optional, the object brace-matched string-aware, and the body bounded at the **next opener** — without which a truncated call can absorb the closing brace of the one after it and dispatch a real tool with another call's text as arguments. `ToolCallGate` is the streaming half: it withholds a call from the display and hands the parser **exactly the bytes it withheld**, so the display and the parser cannot disagree about what a tool call is. Its `flush` is the safety net — a span that opened like a call and parsed as nothing comes back out as text, because no answer, no tool and no error is the least debuggable outcome there is. |
| `gemini_cli_events.h/.cpp` | Wire→typed-event mapping for `gemini -o stream-json`. Real deltas, and they split anywhere — one recorded boundary fell inside a two-digit number — so text is concatenated with no token or line assumption. Also drops the user turn the CLI echoes back, and picks the answering model out of the per-model usage breakdown. |
| `gemini_cli.h/.cpp` | The Gemini provider. **Apogee generates the session UUID** and passes `--session-id`, then `--resume <same uuid>` — the only backend in the family with nothing to capture from the stream. **Pins `--approval-mode plan` AND `--skip-trust`**, which are inseparable: without the second the CLI silently downgrades the read-only pin in an untrusted folder, and refuses to run headless at all. |
| `ollama_cli_output.h/.cpp` | Splits `ollama run`'s stdout into thinking and answer. That CLI emits no typed events — reasoning arrives in-band between literal markers — so this is the one place Apogee pays for in-band markers, bounded by two rules that keep a model's own prose from being mistaken for framing. |
| `ollama_cli.h/.cpp` | The Ollama provider. **Pre-flights for a running server and refuses if there is none**, because `ollama run` starts one itself — see the header. No persistent child and no turn protocol: each turn re-sends the whole conversation. |
| `factory.h/.cpp` | Config `type:` → provider, and `build_providers()` which registers every entry on a Harness and installs the router. Lives here, not in a command, because every surface needs it. |
| `embedstore/store.h/.cpp` | The chunk store. **External-content FTS5**: the text lives once in `chunks`, and three triggers keep the index in step on every write — storing it twice would double a corpus on disk and create a second copy to fall out of step. All migrations run in **one transaction**, so a first open that is killed midway leaves the whole schema or none of it. `verify_index()` passes `integrity-check 1`; without the `1`, FTS5 checks only that the index is internally well-formed, never that it agrees with the table it mirrors. Schema v3 (Milestone Y) adds the nullable `metadata` column on the same migration path; `update_metadata` names only that column, so the FTS trigger rewrites the same text and the vector is not on the statement at all. `search` with a limit of 0 or less returns every match (SQLite's `LIMIT -1`), for a caller that filters before it cuts. The graph members declared here are defined in `graph.cpp`, `graph_search.cpp`, `graph_communities.cpp` and `graph_dedupe.cpp` (see the package row); `search` with a limit of 0 or less returns every match. |
| `embedstore/fts.h/.cpp` | Query text → an FTS5 `MATCH` expression, and BM25 → a comparable score. Terms are quoted (a bare `NOT` or `-` is FTS5 syntax, and a user's question contains both) and joined with **OR**, which was found by running it: implicit AND returned zero rows for "what is the capital of France" against a corpus that answers it, because a question carries words its answer does not. BM25 is unbounded and negative, so `s/(1+s)` maps it to 0..1. |
| `embedstore/chunk.h/.cpp` | Splitting on **codepoint** boundaries, not bytes — a chunk that ends mid-sequence puts invalid UTF-8 into the index and into a model's context. That is one test, `(byte & 0xC0) != 0x80`, which is the whole of what utf8proc would have been taken on for. Overlap is clamped below the chunk size: a config value must never be a value that hangs the process. |
| `embedstore/ingest.h/.cpp` | The directory walk. Skips binaries on a NUL byte in the first 8 KiB (grep's test), and **names every skip** — silently omitting a document leaves a user certain it is in there. `pdftotext` is Apogee's one optional external binary; a PDF it cannot read is skipped by name while the rest of the directory ingests. Sources are recorded relative to the ingest root, so a corpus survives being moved. |
| `embedstore/vector.h/.cpp` | Vectors as little-endian float32 blobs, cosine (0 across widths -- the last line, not the policy), and `fuse_rrf`: Reciprocal Rank Fusion with k = 60 and each half fetched 50 deep, reading only ranks because BM25, cosine and RRF are three incomparable scales. Ties break by chunk id so fusion is deterministic. |
| `agentloop/rag.h/.cpp` | Retrieval → a transient system prefix. The excerpts are **framed as retrieved excerpts** explicitly: without that framing a model reads an injected document as something the user said, and the more relevant the retrieval the more confidently it answers about the document instead of the question. A missing collection or a store that throws sets `error` and returns no prefix — answering without context beats refusing to answer. `retrieve_for_turn` is the turn-level entry: facts → one decision → exactly that search → optional judge → the transient prefix, with `reranked` and `notes` reported. Since Milestone Y's third slice `RagTurn` carries the collection's `graph:` knobs and `retrieve_for_turn` expands through `graph_context` before the judge (see the package row); `render_rag_context(chunks, graph_section)` frames the section on its own when a judge dropped every chunk. Since the fourth slice the turn walks whichever store `resolve_turn_graph` named -- a named graph's own database, opened for the walk alone, with the member's label on the seeds and the graph's name on the header. |
| `agentloop/embed_func.h/.cpp` | `Embedder`: a function from texts to vectors plus the three facts the resolver needs -- which model (so a store's recorded space can be matched), how wide, and whether each call is **metered**. Resolved through the embedding role chain and `Harness::embedder_for`, never a type; injectable, so every resolution test runs without a backend. |
| `agentloop/retriever.h/.cpp` | **The one per-turn resolver** -- see CLAUDE.md → *⚠ One retriever per turn*. Pure functions over facts: `resolve_turn_retriever` (flag > pin > auto; hard error for an impossible explicit ask, exclusion for an unqualified pin, honest lexical for a degraded hybrid, wholesale demotion with the re-ingest hint) and `resolve_ingest_retriever`, which carries the user's spend rule. `valid_retriever` is the single validator behind the flag, `check`, and `/retriever`. |
| `agentloop/rerank.h/.cpp` | One judge generation call reorders the widened candidate set (10× the limit, capped at 50, 400-codepoint snippets). **Never fails**: no judge, a provider error, a reply that is not a ranking -- each returns raw order with `applied = false`; an empty array is a verdict (`applied = true`, nothing injected). Asks for a bare JSON array and parses tolerantly; provider-native structured output is deferred, safely, behind that contract. The budget is 1024 tokens because a **reasoning** judge spends most of it thinking first -- at 256, gpt-oss never reached its answer. |
| `commands/embed.h/.cpp` | `apogee embed ingest / query / list / info / delete`. Collections live at `~/.apogee/embeddings/<name>.db` and the name must be a plain one — the path is built, never accepted. The first ingest of a name **registers it under `embeddings:`** through `edit_config_file`, recording the chunking used; later ingests read that chunking back as their default, and a flag wins for one run without rewriting the file. A config that will not load or will not take the edit costs the registration and says so on stderr — never the ingest, which is already on disk. `ingest --retriever` embeds chunks through the resolved embedder under the spend rule and records the binding; `query --retriever/--rerank` runs the same decision the conversational surfaces make; `info` shows the binding and coverage. `collection_path` is also what `graph` and the admin graph routes open. `ingest --graph` chains the covering graph's build after a successful ingest only, from a fresh config so it sees the entry the registration just wrote; ingest's `-m` stays the embedding override. |

**The transport is injected**, which is what makes every cloud-backend test hermetic: no network, no API key, no charges — and failure modes a live endpoint will not produce on demand (a 529, a body delivered one byte at a time, a connection dropping mid-frame). `tests/support/fake_transport.h` is the scripted implementation.

**Two contracts that are easy to get wrong:**

*An error response is never streamed to the sink.* `HttpTransport` accumulates a non-2xx body into `HttpResponse::body` instead. Beyond the obvious (an error body is not what the sink was written to parse), delivering it makes the request look partly answered to `HttpClient`, which then refuses to retry a perfectly retryable 429. `FakeTransport` honours the same rule, or the fake would hide the bug.

*A stream is not retried once bytes have reached the caller.* Replaying would deliver the first half of an answer twice, with no way for the caller to tell.

**SSE never arrives one-event-per-read.** Chunk boundaries land mid-frame, mid-line, and mid-UTF-8-sequence. `SseParser` is a state machine with a carry buffer for exactly that reason, and its test replays a recorded stream at *every* chunk size from one byte up — a parser that assumes one-read-one-event passes every test written on a fast local connection and drops tokens on a slow one.

**The local backend filters at the source, and the order is load-bearing.** `LlamaCppProvider::generate` runs each generated piece through `ThinkFilter` → `ToolCallGate` → `MarkupFilter` before it reaches the token callback, the returned text, or persisted history — filtering at one surface and not another is how a marker hidden on screen reappears in a saved transcript. The order is not arbitrary. For gpt-oss the reasoning block's opener **is** a header (`<|channel|>analysis<|message|>`), so stripping headers first would leave the block with no boundary and put the model's working into its answer; and the gate keys on `<|channel|>commentary to=`, half of which the markup filter would have eaten. Three ctest cases pin the three orderings, driven through the provider rather than through the filters, because a unit test over the filters would be re-implementing the order it is checking.

**Adding a local-inference capability.** Extend `LlamaRuntime`/`LlamaModel`/`LlamaContext` in `llama_runtime.h`, implement it in `llama_real.cpp` under the `APOGEE_ENABLE_LLAMA` guard, and add it to `tests/support/fake_llama.h`. The merge-blocking target has no llama.cpp in it, so the fake is not a convenience — it is the only place these behaviours are tested at all. Be aware of what it cannot model: it has no allocator, so batch and context limits are honoured by asking the seam for them rather than by assuming, and both of those rules exist because a real GGUF broke a build the fake called green. To replay a recorded transcript, script generation with `FakeLlamaRuntime::script_text` — a list of the exact **pieces** a model emits, one token each. Token ids are the wrong unit for that: a real tokenizer splits framing where no word-splitting fake would (gpt-oss emits `commentary` as `comment` + `ary`, straight through the middle of a marker), and a streaming filter tested only on whole markers has not been tested. **And run it against a real GGUF before believing the green**: the fake has no allocator and no micro-batches, and both of the embedding path's real bugs — a default `n_seq_max` of one that aborted the first call, and equal-length micro-batch splitting that pooled a text over a fragment of itself — were invisible to it and found in minutes by a scratch program linking `apogee_core` from the llama build.

**Adding a cloud backend.** Reuse `HttpClient` and `SseParser`; put the dialect in its own `*_wire.h/.cpp` so it is testable with no transport at all. Take an injected `HttpClient` in the constructor. Honour cancellation between chunks. Keep the API key in a header and out of every error message — `tests/backends/anthropic_test.cpp` → `[backends][anthropic][secrets]` is the pattern for pinning that. Then add a row to `providers()` in `tests/agentloop/conformance_test.cpp`: a backend is not done until it drives the shared loop identically to every other.

### `agentloop/` — the shared loop

| File | Purpose |
|---|---|
| `loop.h/.cpp` | `run()`, `Options`, `RunResult`. The model→tool→model cycle. `RunResult` carries the final call's `finish_reason` and the reported `usage` split, because a served response passes both to a client. `Options::side_request` (Milestone Y) marks every request of the run as not a turn of the caller's conversation — the knowledge clerk on a live chat's model. |
| `reporter.h` | The observer every surface adapts. `NullReporter` discards everything. |
| `content.h/.cpp` | `TokenCount`, `splice_transient`, `compact_history`, and `ContextUsage`/`measure_context` — the 80/90 thresholds and the exact-or-estimated count they fire on, shared by the chat REPL, a driven chat, and a served session. |
| `question.h/.cpp` | `ask_user`: schema, validation, answer encoding. |
| `structured.h/.cpp` | Structured output (Milestone X): `validate_against` and `validate_schema` over the draft-07 validator (every violation collected with its pointer), `extract_json` (fences stripped, prose around the object tolerated), `schema_instruction`, `correction_message`, and `run_structured` — the schema on every request, one corrective turn as durable history, and a second miss delivered raw with `conforms: false`. |
| `review_context.h/.cpp` | The arbitrary-branch review (Milestone X): `ReviewContext`, `parse_branch_arg` (`off`, `base..head`, a bare head), `review_note` ("review X compared to Y WITHOUT checking it out"), `review_summary`, `compose_system_prompt`. Plain data the commands turn into the git tools' defaults — no environment side channel. |

**Why it exists before the surfaces.** Extracting the loop behind an observer *before* front-ends multiply is Ommi's most load-bearing sequencing lesson — it is what kept four surfaces consistent there and made deleting one a local change. A surface is a thin adapter over `Reporter`; the loop knows nothing about terminals or HTTP. Three adapters exist, and each is the whole of its surface's rendering: `commands/cli_reporter` (escape codes), `commands/json_reporter` (JSONL), `httpserver/sse_reporter` (SSE frames).

**Three contracts worth knowing before you touch it:**

*`ask_user` is advertised if and only if there is someone to answer it.* A null `AskFn` means the tool is **absent from the request**, not advertised-and-refused. A model told it may ask questions on a surface with nobody attached will ask one, then hang or invent the answer. `advertised_tools()` is public so the rule is testable as a contract.

*Tool problems are results, never exceptions.* An unknown tool, a bad argument, a tool that threw — all come back as error results the model reads and reacts to. Aborting the turn would throw away the conversation over a hallucinated name.

*An aborted tool phase rolls back.* History resizes to the mark taken before the phase, because an assistant message whose tool calls were never answered is rejected outright by several providers.

**Transient content** rides `splice_transient` into the outgoing request and never into history. If it landed there it would be re-sent every later turn, growing the prompt without bound and feeding the model material it was told applied to one question.

### `agent/` — tools

| File | Purpose |
|---|---|
| `tool.h/.cpp` | `Tool`, `ToolRegistry`, `dispatch()`, and the ask/allow/deny permission gate. |
| `fetch_url.h/.cpp` | The `fetch_url` tool and its HTML-to-text stripper, behind an injected `UrlFetcher`. |

**Adding a tool.** Construct a `Tool`, set `writes` if it is destructive, and register it. A `writes` tool goes through the permission gate; a read-only one does not, because prompting for every read trains the user to approve without looking. `Ask` with no confirm function resolves to **deny** — a pipe or a cron job has nobody to ask, and allowing because no one objected is the wrong direction to fail.

**There is no local web-search tool, by decision** (2026-08-26). Search comes from the providers' own server-side tools; Ommi's DuckDuckGo scraper is not ported because a results-page parser breaks silently and returns nothing rather than erroring. `fetch_url` — reading a URL you were *given* — carries none of that fragility.

### The terminal UX layer

`CliReporter` is the only **terminal** `agentloop::Reporter` in the tree, and it should stay that way — a second terminal renderer drifts from the first, and a capability that reaches one surface but not another is the parity bug the interface exists to prevent. A new interactive surface constructs one; it does not write its own. (The other two adapters render to other media — JSONL and SSE — which is the one legitimate reason for another `Reporter`.)

**Answer to stdout, progress to stderr.** They are separate streams and separate objects on `CliReporter::Options` for that reason. Conflating them puts spinner frames into `apogee complete "…" | jq`.

**Everything transient goes through `StatusLine`,** including startup notices — "startup speaks on one line" is an invariant Ommi retrofitted (OMMI-14) and this project designs in. Do not reach for `std::cerr` on an interactive path; call `reporter.status().print_line()`. A permanent print bumps a generation counter so an in-flight spinner repaint drops its frame instead of landing on top of the text.

**The thinking view's erase arithmetic is exact only because nothing painted may wrap.** `repaint()` leaves the cursor on the last painted row with no trailing newline, and `erase()`'s row count depends on that. Pre-wrap to `width - indent` by codepoint. If you change one, a test asserting the exact count of `\033[A\033[2K` pairs will tell you.

**Colour is decided once,** in `ansi::resolve_color()`, across three independent inputs: `NO_COLOR` (by presence, whatever its value), an explicit `--no-color`, and whether stdout is a terminal. A disabled `Style` returns its input unchanged so no caller branches on it.

### `httpserver/` — `apogee serve`

| File | Purpose |
|---|---|
| `http_types.h/.cpp` | `HttpRequest`, `HttpResponse`, `StreamBody` — the transport-neutral shapes every handler speaks — and the OpenAI error envelope every refusal takes. Since Milestone Y also `HttpError` and `to_response`, public so the admin plane can catch what the inference plane's `resolve_served` throws. |
| `mux.h/.cpp` | The route table for both planes: method + path → handler method, `{id}` capture, 404/405 in the error shape. The `/v1/admin/` prefix is gated **before routing** (`401` whether or not the path exists) and mounted only when the mux is given an `AdminHandler` and a token. `Mux::routes()` exposes the table with each row's `admin` flag; `tests/http_api_conformance.cmake` reads it off the source. |
| `handler.h/.cpp` | One method per route. A chat request — streamed or not, tools or not — is one `agentloop::run` behind a `NullReporter` or an `SseReporter`. `resolve_served` is the model → backend rule (unknown → 400 naming the served set; vendor CLI → 400 saying why; configured-but-unbuilt → 503 with the reason). Inference requests serialise behind one mutex. `harness()` exposes the plane's harness for the admin knowledge routes. |
| `sse_writer.h/.cpp` | SSE framing (`data: <json>\n\n`, `[DONE]`), the chunk shapes, `status_event_json`, and `SseWriter`, which cancels the turn's token when a write fails because the client left. |
| `sse_reporter.h/.cpp` | The `Reporter` → SSE adapter. Content deltas always; meta-frames (`delta.content == ""` plus `meta`) only with `apogee_events`. Reasoning is dropped here, deliberately. |
| `session.h/.cpp` | `SessionStore`: server-owned conversations held as `logger::Session` objects and persisted through `logger::save` after every turn, so `apogee chat --resume <id>` reads them. TTL eviction is from memory only, on an injectable clock. |
| `admin_auth.h/.cpp` | The per-install admin token (64 hex characters, a sibling of `config.yaml`, written `0600` before it is renamed into place), `constant_time_equal`, `bearer_of` (header-only, never the query), and the `401`. |
| `admin.h/.cpp` | `AdminHandler`: one method per `/v1/admin/*` route, delegating to the config slice, the job registry, and the event stream. Holds the startup config snapshot `restart_required` compares against. |
| `admin_config.h/.cpp` | The config slice as free functions over a config path — so the byte-identity test calls exactly what the route calls: `admin_create_backend` (`append_backend`), `admin_delete_backend`, `admin_set_role` (`set_models_role`), `admin_format_config`; `backend_view`, the type with no `api_key` field; `config_drifted`; `is_literal_api_key`. A literal key is accepted from a loopback peer only. Since Milestone V also `admin_list_permissions` and `admin_put_permission` (the `set-permission` twin through `set_permission`; `restart_required` when the level differs from what the server started with). Since Milestone W also the `mcp-servers` slice: list/get views with `env_set` and never the values, create through the scaffold core (`409` on collision), delete, and enable/disable through `set_mcp_server_enabled`. |
| `admin_agents.h/.cpp` | The agents slice (Milestone X) as free functions over a config path: `admin_list_agents` and `agent_view` (the bundled three and every entry, `bundled`/`overrides_bundled`), `admin_create_agent` and `admin_put_agent` through the scaffold core (`409` on collision, `PUT` with `force` implied and the name from the path), `admin_get_agent` inlining `prompt_bodies`/`schema_bodies` (the compiled-in text for an unseeded bundled agent, `present: false`), `admin_delete_agent` with `?purge=true`. No `restart_required`: `analyze` reads the config per run. |
| `admin_knowledge.h/.cpp` | The knowledge slice (Milestone Y) as free functions over a config path and the inference plane: `admin_capture_knowledge` — the `knowledge capture` twin, the clerk's backend resolved as a chat request's `model` is, `501` when the server serves no generation backend, `502` when the clerk failed twice, `400` for everything the CLI refuses, and `draft: true` running everything but the store — `admin_create_knowledge`, storing a finished record without the clerk (an id and a timestamp minted when absent, kept when given), `admin_refine_knowledge` (the stateless revision pass, its guards before the clerk), `admin_list_knowledge` (a missing collection an empty list that creates nothing; `?q=` through `knowledge::query`; `501` for an explicit vector ask with no embedder), `admin_get_knowledge`, `admin_patch_knowledge` (exactly one of `link` or `status`), `admin_delete_knowledge`, and `admin_reindex_knowledge`. All through `commands/knowledge_core` and `knowledge/`, so the record is the CLI's record. The literal paths (`capture`, `refine`, `reindex`) sit before the `{id}` rows in the table. `?graph=true` on the list route adds the walk from the matched records. |
| `admin_auth_routes.h/.cpp` | The credential slice as free functions over a config path: `admin_list_credentials` (metadata plus each cloud backend's key source — `GET /v1/admin/auth`), `admin_put_credential` (the `auth add` twin — key in the body only, and **loopback peers only**, judged from `remote_address` before the body is read), `admin_clear_credential` (the `auth clear` twin). |
| `jobs.h/.cpp` | `JobRegistry`: `202`-style async work — `start`/`progress`/`finish`/`fail`/`cancel`, each an `admin.job.*` event; `finish` and `fail` are no-ops once a job left `running`, so cancel wins. `JobWriter` turns lines into progress. `JobWorkers` (Milestone Y) is the thread holder a route's async work runs on: destruction cancels every job's token first, then joins, so no worker outlives the registry, the plane or the harness. |
| `admin_events.h/.cpp` | `GET /v1/admin/events`: named SSE frames from the bus, a heartbeat, a poll that notices shutdown. |
| `shutdown.h/.cpp` | The atomic flag a signal handler can set and a blocked stream can read, so Ctrl-C ends an idle event stream within one poll. |
| `serve.h/.cpp` | The bind policy (`bind_refusal` — fail-closed behind `--allow-remote`; `is_loopback_host` lives in `http_types` so the admin slice can judge a peer) and `run_server`: cpp-httplib behind the mux, the sweeper thread, SIGINT/SIGTERM → `stop_serving()`. **The only translation unit that may reference `listen()`/`accept()`**; the symbol check allow-lists `serve.cpp.o` by name. |

**Listener-free by design.** Everything above `serve.cpp` works in `HttpRequest`/`HttpResponse` terms, so the conformance suite (`tests/httpserver/handler_test.cpp`) drives every route — a streamed turn included, frame by frame — with no socket, no thread pool, and no timing. `serve.cpp` is a thin translation layer in front of the mux.

**The contract for remote clients** is [`documentation/reference/http-api.md`](../reference/http-api.md). Read it before changing a route or a field; `cli.http_api_conformance` fails the build when the route table and the document disagree.

**Sessions are asked for, never implied.** A request without `session_id` is stateless, exactly as a stock OpenAI client expects; `"session_id": "new"` mints one; an unknown id is a typed 404. A served session is a chat session on disk from birth.

### `logger/` — sessions and the operational log

| File | Purpose |
|---|---|
| `session.h/.cpp` | `Session`, serialize/deserialize with resume warnings, and the save/load/list filesystem layer. Since Milestone Y also `transcript_text`: user and assistant turns as plain text, nothing else. |
| `operational.h/.cpp` | The daily append-only log. One greppable line per event. |

**The session file is rewritten after every completed turn.** Crash safety is the requirement — a `kill -9` mid-conversation must leave every finished turn on disk — and that is a property of *when* the write happens, which is why persistence was specced with the REPL rather than added afterwards. The write goes through `harness::write_file_atomically`, so an interrupted write leaves the previous session intact.

**Resume degrades, never fails.** A legacy schema, a vanished backend, a field of the wrong shape, an unreadable message — each produces a `ResumeWarning` and a working session. A conversation that cannot be reopened because one config key moved is worse than one that reopens with a note. `deserialize` throws for exactly one thing: JSON that will not parse at all.

**`chat_id` is immutable.** Renaming sets `custom_name`. An id that changed would break every reference to the session that already exists.

**The operational log never throws.** A full disk degrades to a missing log line, never to a failed conversation.

### `commands/chat.cpp` — the REPL

**Context is measured against the request about to be sent** — the saved history *plus* the incoming turn — and compaction folds only the prior history. Measuring the saved history alone means the first turn always reads as empty and a single large prompt never trips the threshold; that bug shipped once and `context is measured against the message about to be sent` now pins it.

**Every configured backend is constructed at startup.** That is what makes `/model` an instant switch with history carried over, and it only works because history is neutral IR rather than a vendor transcript.

**Input is read through `LineReader`,** which has two implementations rather than one reader with a conditional: `make_line_reader` builds the replxx editor only when **both** stdin and stdout are terminals. Both, because replxx draws on stdout — a terminal stdin with a redirected stdout would write escape sequences into the redirect, and that redirect is the answer the user asked for. A piped conversation is a first-class way to use `apogee chat` and gets `std::getline`, tested on its own.

**Beware when writing a PTY test against the REPL: replxx puts the terminal in raw mode, where Enter is `\r`, not `\n`.** The kernel performs no translation, so a bare `\n` lands in the edit buffer as literal text rather than submitting the line. Both PTY harnesses send `\r`, and both were briefly wrong about this when line editing landed.

**The typeahead flush runs exactly once, before the first prompt.** Never between turns — typing a follow-up while the model generates is legitimate typeahead, and eating it would be worse than the problem the gate fixes.

**The REPL and the machine-mode loop share `run_chat_turn()`.** One function, called from both — not two loops that agree today. The two differ only in how a turn *arrives* (a `LineReader` line versus a JSONL `user` message) and how it is *rendered* (`CliReporter` versus `JsonReporter`); everything between is identical by construction, which is what makes "a driver sees what a terminal user saw" a structural fact rather than a promise. `cli.reference_driver` checks it anyway.

**`/capture` and `auto_capture` go through the capture core with the loaded model as the clerk.** The clerk runs as a side request with a throwaway history, so the conversation's own history — and a local model's cache — is untouched; the session's `--retriever` describes how its documents are searched and says nothing about how a record is indexed, so only the collection's pin and auto apply. `auto_capture` fires when the REPL ends cleanly (`/exit`, `/quit`, the end of the input) and not on an interrupt, which `LineReader::interrupted()` reports; machine mode has no slash commands and no auto-capture — a driver has the CLI and the admin route.

**The two format flags must agree, and disagreeing is an error.** `--input-format` defaults to whatever `--output-format` is, so the ordinary cases need one flag. Mixing them is refused: a JSONL-emitting REPL has no coherent meaning — slash commands print through the terminal reporter and have no protocol event — and a driven session rendering prose gives its driver nothing to parse. Both mixed cases silently ignored a flag before the check existed.

---

## `lib/src/cli/tests/` — tests

Catch2 v3, discovered into ctest by `catch_discover_tests`. The directory mirrors `source/`'s packages.

| Path | Covers |
|---|---|
| `smoke_test.cpp` | Version stamping and that dependencies are linked and usable. |
| `packages_test.cpp` | Every reserved package header compiles and is includable. Since Milestone X also `render/`, `scaffold/agent.h` and `harness/assets.h`. |
| `platform/platform_test.cpp` | Host detection and the five-target name vocabulary. |
| `commands/registry_test.cpp` | Registration, lookup, ordering, duplicate and null rejection. |
| `commands/root_test.cpp` | The real parsing path: help, subcommand dispatch, `--config`, unknown commands. |
| `harness/config_test.cpp` | The loader: typed parsing, `${ENV}` expansion, case-collision rejection, the template byte-match, and a garbage-input battery asserting a message rather than a crash. Since Milestone X also `agents:` (every field, both enums validated, relative paths left for the loader, `${ENV}` and `~` expanded, collisions) and `is_vendor_cli`. Since Milestone Y also `knowledge:` — `auto_capture` and `db` parsed, the default collection, a non-boolean and a pathed name refused, the template documenting the section. `graphs:`: file order, defaults, the hops and max_entities validation, a list required, a fold collision refused, the template's section commented out. Since the training pipelines item `training.pipelines`, `training.regimes` and `training.cycle`: every field and default (strict no-regression, the breaker at 3), `~` and `${ENV}` expanded in the paths, `configured()`, the template documenting all three unset, and every load-time refusal naming its key -- a stage without a name, dataset or eval suite, a bad method, a negative count, a rehearsal fraction or threshold out of range, an unknown source type, a `sessions` source without `log_consent: true` (naming privacy and self-reinforcement), a bad `since` date; and the spec-file parsers with the stem as the fallback name and invalid YAML refused. |
| `harness/config_edit_test.cpp` | The golden-file suite: byte-identical round trips over a comment-dense fixture, comment ownership on delete, CRLF and final-newline handling, and the atomic-failure paths. Since Milestone X also the agent entry: alphabetical after the name with only what is set, `tools` always, delete the inverse. `set_embedding_graph_enabled`: the block appended, the value replaced in place keeping the comment, a block without the field, the `backends:` twin untouched, refusals. `append_graph`/`delete_graph`: a byte-exact round trip, only what is set written, the fold and duplicate rules, `force` in place, and the **section-scoped delete leaving a same-named agent and MCP server intact**. |
| `harness/paths_test.cpp` | `APOGEE_HOME` resolution, the `~/.apogee` default, and the no-home error. |
| `harness/types_test.cpp` | The IR: both content shapes round-tripping, tool calls, typed errors on bad input, and the transient-exclusion contract. |
| `harness/harness_test.cpp` | Router precedence table-tested across all three rungs, cancellation mid-stream, capability probes, `ModelBehavior`, and the context-window table. `generation_is_metered`: the mock no, an unroutable name yes. |
| `backends/mock_test.cpp` | MockProvider contract tests — they pin the `LLMProvider` interface itself, so a later item changing it breaks them. Since Milestone X also the script (turns, tool calls, generated ids, refusals by name) and the placeholders, streamed and not. |
| `backends/sse_parser_test.cpp` | The SSE state machine, replayed at every chunk size from one byte up. |
| `backends/http_client_test.cpp` | Retry/backoff, `retry-after`, the no-retry-after-delivery guard, and the error-bodies-are-not-streamed contract. |
| `backends/anthropic_test.cpp` | Fixture-replayed streams (seven chunk sizes), the wire mapping, thinking replay, error shapes, and the API-key secrets guardrail. Since Milestone X also structured output: the version parse, the native field with its beta header, the forced tool (`tool_choice` by name, `any`, none under thinking), and the fold in both paths leaving a real call intact. |
| `backends/factory_test.cpp` | Construction per type, that a keyless cloud type names **its own** environment variable, and that one unbuildable backend does not stop the others. The API-billing providers state metered generation; the mock does not. |
| `backends/llamacpp_test.cpp` | The local backend against a scripted runtime: KV reuse as an exact token count, side-request isolation, batch chunking, the context wall, idle unload, load failure, and the vision capability. Since Milestone X also the schema stated once in the prompt and never twice, by exact token counts. |
| `backends/chat_template_test.cpp` | Local prompt framing, the conservative name-matching registry, and that a model's own template wins. |
| `backends/jsonl_framer_test.cpp` | The line framer directly: every chunk boundary, a newline landing exactly on one, CRLF, a bounded buffer, and a 100 KB line (a real `init` event exceeds 64 KiB with MCP servers configured). |
| `backends/claude_cli_events_test.cpp` | The fixture-replay suite: every fixture at 1/2/3/7/64/4096-byte chunks must produce an identical event sequence. Fixtures are **transcribed** from the verified wire table, not recorded — see Milestone L. |
| `backends/claude_cli_test.cpp` | The provider against a scripted child: one child across turns, only-what-is-new delivery, resume-then-replay, side-request isolation, schema suppression and its prose fallback. |
| `backends/codex_cli_test.cpp` | The Codex provider against **recorded** ChatGPT-session output: the sandbox pin (and that no dangerous escape can appear), resume's narrower flag surface, thread capture and resume, side-request isolation, and that a long answer really is one event. |
| `harness/roles_test.cpp` | The resolution chain, table-tested rung by rung, plus the compatibility promise: with every role pointer unset the chain is byte-identical to the pre-role behaviour every caller used to inline. |
| `models/gguf_inspect_test.cpp` | The header reader against **built** fixtures rather than committed ones — the cases that matter are the malformed ones (truncated download, length running past the end, Git LFS pointer), and those have to be constructed, not downloaded. |
| `models/acquire_test.cpp` | **The failure-injection suite.** Each rung of the ladder is broken in turn — truncated stream, wrong size, wrong digest, bytes that are not a GGUF — and each case asserts the same three things: the transfer failed, nothing is visible, and no `.partial` survived. The third matters most: a leftover partial is how a later run "resumes" bytes of unknown provenance. |
| `models/sha256_test.cpp` | Known-answer vectors at every block boundary, all taken from an independent implementation. One expectation in the first draft was written from memory, was wrong, and the code was right — which is the argument for taking every vector from a reference. |
| `models/sidecar_test.cpp` · `models/source_ollama_test.cpp` · `models/source_hf_test.cpp` | The record's honesty (an absent digest must not read like a failed one), the store layout against a fixture store, and the refuse-rather-than-guess behaviour against a fake transport. Since Milestone Z the `/tree` listing with LFS digests and sizes for models and datasets, the dataset download URL and promise, the snapshot and dataset file filters, the directory name, and the GGUF-less refusal naming `--safetensors`. |
| `commands/models_pull_test.cpp` | The boundary the mutating verbs must never cross: "delete a model by name" can never become "delete a file by path". |
| `commands/models_test.cpp` | The listing surface: that the ROLES column comes from the shared resolver, that a cloud backend is not reported as a missing file, and that an unreadable header states its reason **on the failure line** rather than anywhere in the body., and that a cloud row's state column names where its key comes from — through the one resolver — or `no key` with the `auth add` remedy Since Milestone Z a SafeTensors snapshot listed as trainable with its architecture and record, under `models/` and under `paths.hf_dir`. |
| `backends/think_filter_test.cpp` | The filter against **verbatim observed output**, replayed at every chunk size, plus the two rules earned elsewhere: a partial marker at end of stream was never a marker, and an empty pair list cannot underflow the hold-back. |
| `backends/model_profile_test.cpp` | The ladder rung by rung, and the honesty checks: every profile must carry evidence, an unverified one must say so, and the verified set must be exactly the families actually run. |
| `backends/gemini_cli_test.cpp` | The Gemini provider against **recorded** Google-login output: that the read-only pin and its trust escape are always both present (and no escape ever is), that `--raw-output` never appears, session naming vs resuming, the echoed user turn never reaching the answer, deltas reassembling exactly across a mid-number split, and tool activity going to the status channel. |
| `backends/ollama_cli_test.cpp` | The Ollama provider against **recorded** output from a real signed-in cloud session: that it refuses without spawning when no server is reachable, that no argv it can build says `serve`, that `--nowordwrap` is always passed, and that thinking never reaches the answer at any chunk size. |
| `platform/child_process_test.cpp` | The process seam against real `sh`/`cat`: stdin round-trip, stdout/stderr separation, read timeouts, exit status, and that writing to a dead child returns false rather than killing us. |
| `backends/openai_test.cpp` | The Responses dialect, split-boundary streaming, tool-call reassembly from item + argument deltas, effort banding, error shapes, and the API-key secrets guardrail. Since Milestone X also `text.format` beside the tools, never `response_format`. |
| `backends/openai_embed_test.cpp`, `google_embed_test.cpp` | Each translator on its own, then the provider over the scripted transport: the endpoint and header, rows by index (OpenAI) or in order (Gemini), **the batch-splitting boundary** (2049 inputs → 2048 + 1; 101 rows → 100 + 1), a 429 retried, an error that names the problem and never the key, the entry's `embedding_model` honoured, an unknown width learned from the first vector, and `can_embed` answered through the harness. |
| `backends/llamacpp_embed_test.cpp` | In-process embeddings over the scripted runtime: one vector per text in order, texts handed to the runtime in bounded slices (130 → 64, 64, 2), width unknown before load and exact after, a runtime failure as a `ProviderError`, the idle timer reset by an embed, and two runtimes of different width as the dimension-mismatch fixture. |
| `backends/embedding_batch_test.cpp` | `batch_ranges` as a table, including the zero-maximum case that would otherwise be an infinite loop. |
| `agentloop/retrieval_test.cpp` | The resolver as an exhaustive table: auto across every embedder × store combination, auto-never-hybrid, wholesale demotion, the explicit-vector hard error, vector-pin exclusion, flag-beats-pin, hybrid's honest degradation, and the ingest spend rule. Plus `resolve_embedder` against a harness holding a free mock, a metered OpenAI entry, and a chat-only backend. |
| `agentloop/rerank_test.cpp` | One case per judge failure path, the applied verdicts (reorder; drop everything), the tolerant parser, judge resolution, and the widen/cap arithmetic. |
| `embedstore/vector_test.cpp` | Blobs, cosine, RRF arithmetic and determinism, vector and hybrid search against a store, mixed widths, the binding, and **the v1 → v2 migration against `tests/fixtures/embedstore/schema-v1.db`** -- a real file the previous build wrote, not a hand-made schema. |
| `support/embedding_fixtures.h` | Both vendors' native widths and documented-shape response bodies of deliberately different, deliberately small widths — for `vector-hybrid-rerank`'s per-store binding tests, and consumed by the embed tests here so the file is not dead. |
| `backends/google_test.cpp` | The Gemini dialect, thought-vs-answer separation, split-boundary streaming, `functionResponse` round-trip, and the API-key secrets guardrail. Since Milestone X also `responseSchema` sanitised at every depth and absent beside tools. |
| `agentloop/conformance_test.cpp` | **The cross-provider table.** The same two scripted turns in four dialects driving the same loop: identical answer, iterations, history shape, tool linkage, and usage. Also pins that a mid-session `/model` switch carries a full history — tool call and result included — across every translator. |
| `commands/helpers_test.cpp` | Flag/config resolution order, base64 padding, image parts, message assembly. `describe_retrieval` names `+N graph entities`; `retrieve_for_collection` carries the collection's `graph:` knobs from the config into the turn. |
| `agentloop/loop_test.cpp` | **The conformance suite** — multi-tool turns, tool errors, unknown tools, the iteration bound, `ask_user` advertisement and rollback, the permission gate, transient exclusion, and the Reporter sequence. Every later provider must pass it. |
| `agentloop/anthropic_loop_test.cpp` | The real Anthropic provider driven through the real loop on recorded SSE — the join the unit suites do not cover. |
| `agentloop/content_test.cpp` | Token estimation, splicing, compaction (including that a failed compaction returns history unchanged), and `measure_context`: the 80/90 thresholds, an unknown window never firing, and measuring against the message about to be sent. |
| `agentloop/question_test.cpp` | `ask_user` schema, validation messages written for the model, answer encoding. |
| `agent/tool_test.cpp` | Registry, permission resolution, dispatch, and `fetch_url` including its HTML stripper. |
| `ansi/ansi_test.cpp` | The colour mode matrix, table-tested across all eight combinations. |
| `commands/thinking_view_test.cpp` | Byte-level: wrapping, the rolling window, the erase arithmetic, and that nothing but the summary survives `finish()`. |
| `commands/status_line_test.cpp` | Overwrite, clear, the generation counter, verbosity modes, and the spinner frame format. |
| `commands/cli_reporter_test.cpp` | The stdout/stderr split and that thinking never reaches the answer stream. |
| `commands/machine_mode_test.cpp` | The JSONL protocol: the event vocabulary, that every line is an object with a `type`, that absent usage is omitted rather than zeroed, the driver-side **adversarial-chunk replay** (1/2/3/7/4096-byte reads yield identical events — the discipline Apogee demands of the CLIs it drives, owed to its own consumers), and `ask_user` over the wire including the hang-up case. |
| `commands/chat_test.cpp` | Slash parsing, title sanitising, and the log line format. |
| `commands/knowledge_test.cpp` | `knowledge capture` and chat's `/capture` in process on the scripted mock: one chunk keyed by its id with the archive and the registration, the name unreachable by search; every override and `--supersedes` flipping the earlier record (a missing target a note); a dry run's zero footprint by a helper reading the collection, the archive and the config's bytes, and its decision equal to a real run's; the refusals (no input, a bad status, a pathed `--db`, a vendor CLI, an unknown `-m`, a clerk that never conforms exiting 2) storing nothing; piped stdin; `--from-chat` field-equivalent to a capture on the same transcript with `source: chat`; `--db` and an unmetered embedder vectorising under auto and `--retriever vector` but never under lexical, the binding recorded; an explicit vector ask with no embedder refused and warned about by a dry run; `/capture rejected` and `/capture PROJ-7` on a piped chat with the loaded model as the clerk and the archived transcript the session's own words, nothing to capture before a turn; `auto_capture` on a clean exit only when switched on, and never over an empty session. Since the second slice also the lifecycle: `query` refusing to create a missing collection, shipped by default with the rejected top hits filtered before the cut, every branch and a discipline on request, the JSON envelope, an explicit vector ask refused; `list` newest first and filtered; `info` with and without the archive; `link` and `status` leaving the vector's bytes and the chunk alone; `delete` taking the archive; `export` faithful and anonymized, a Markdown report to a file; `reindex` honest about a lexical collection, warning on a mixed one, rewriting every vector under the resolved embedder and recording it. `query --graph`: the note before a graph exists, the walk after one is built (the entity the record concerns, a sibling decision marked), and the JSON shape. |
| `httpserver/handler_test.cpp` | **The HTTP conformance suite**, through the listener-free mux: the `chat.completion` and chunk shapes a stock client reads, usage present exactly when reported, `[DONE]` last, meta-frames only on opt-in and always on an empty delta, server-side tools with the client seeing only the final answer, `tool_mode`, client `tools` refused, every error in the envelope, the model → backend rule (unknown, vendor CLI, unbuilt, the served default), a provider failure as 502 or an error frame that still ends the stream, a truncated answer reported as `length` in every shape, an empty chunk never ending a stream, the `system` shorthand, sessions (minted, continued with the whole history, resumable from disk, typed 404, list/show/end), compaction of a session near its window versus a stateless request only warned, retrieval injected into the request and never the transcript with `?retriever=` validated and an impossible explicit ask refused, `/v1/completions`, `/v1/models`, `/v1/model/status`, `/health`, and the mux's 404/405. |
| `httpserver/sse_test.cpp` | Framing, `status_event_json`'s optional fields, finish reasons, a failed write closing the writer **and cancelling the token**, the tool-name rule, the frame order a client expects, no frames without the opt-in, and `model_ready` exactly once. |
| `httpserver/session_test.cpp` | The store on an injected clock: persisted at birth, committed turns on disk before memory changes, TTL eviction from memory only, revival by commit, ending a session leaving its file. |
| `httpserver/serve_test.cpp` | The bind policy table (every loopback spelling; fail-closed otherwise) and the route table for both planes. Fifty-nine admin rows since the training pipelines item. |
| `httpserver/admin_auth_test.cpp` | The 401 matrix (no header, wrong scheme, wrong token, one byte off, the right token in the query string), `constant_time_equal`, the token file: generated once, 64 hex, `0600`, regenerated when empty. |
| `httpserver/admin_config_test.cpp` | **The parity proof**: the real CLI run in-process and the HTTP slice on two copies of the shipped template leave byte-identical files after add, set-default, format and delete. Plus: the view never carries the key, a literal key needs a loopback peer and a `${ENV}` reference does not, `409` on collision and `force`, every refusal the CLI makes, `restart_required` on membership and role drift and not on a field. |
| `httpserver/jobs_test.cpp` | A scripted job start to finish with its events; cancel firing the token and a dying worker's `fail`/`finish` changing nothing; a failed job ignoring a late finish; the line writer. |
| `httpserver/admin_events_test.cpp` | Named frames; a stream that opens, delivers, heartbeats and ends when the client leaves; a stream that ends within one poll of shutdown. |
| `httpserver/parity_test.cpp` | **The CLI↔HTTP parity table and its completeness**: every leaf subcommand of the real registry must be classified (twin / backfill / carve-out / read-only), a stale row fails, every twin's route is in the table, flagged admin, and answers `401` unauthenticated; the gate runs before routing and the plane is mounted only when given. Since Milestone X `agents create|edit|delete` are twins and `analyze`/`agents list` read-only; since Milestone Y `knowledge capture`, `link`, `status`, `delete` and `reindex` are twins and `query`/`list`/`info`/`export` read-only; `graph build`, `graph delete`, `graph communities`, `graph dedupe`, `config add-graph` and `config delete-graph` are twins, `graph stats`/`show` read-only. |
| `events/bus_test.cpp` | Delivery after subscription, stamping, a slow subscriber dropping without blocking the publisher, unsubscribe waking a waiter, `close_all`, the RAII subscription, the process-wide bus. |
| `tools/fs_test.cpp` | The sandbox against an escape table — `..`, an absolute path outside, a symlink pointing out, and the `rootX` prefix trick — every one refused naming the root and touching nothing; the read cap and its note; directories first; hidden directories skipped by search; `wildcard_match`. |
| `tools/shell_test.cpp` | `run_command` declares `writes`; stdout, `[stderr]` and `[exit N]` trailers; `cwd` default and override; a quote in the command stays data; a command outliving the timeout is a result within seconds, not a hang. |
| `tools/git_test.cpp` | Against a fixture repository with a bare `origin`: the ref allow-list; log range rules (bare ref means since-ref, 1–200); the review diff between two **un-checked-out** branches is the merge-base diff; a remote-only branch resolved by fetching under `auto`; `never` refusing with the reason; option injection refused before git sees it; the flags' review defaults applying when the model passes no refs and losing to its own. Since Milestone X also `git_log`'s review form and the live review re-pointing both tools without re-registering. |
| `tools/notes_test.cpp` | Keys that can never be paths; round trip, overwrite, sorted listing, delete, and the writers gated. |
| `tools/rag_query_test.cpp` | Over a seeded lexical store with no embedder: every hit names its retriever; an explicit `vector` is an error naming lexical, never a silent fallback; nothing matched is a result; names validated before the filesystem; no RAG tool is destructive. |
| `tools/gate_test.cpp` | **The gate's first real consumer, through the shared loop**: a scripted model reads a file with no gate and is denied a write as a tool result when nobody can confirm; the checker's allow/deny/ask each decide; every destructive native tool declares `writes` and no other does; `tools.disabled` removes a whole toolset. |
| `commands/permissions_test.cpp` | The checker's precedence (config, then the session's answers, then ask; a session answer never overrides a config deny) and the machine-mode prompt over string streams: `yes` once, `no`/unknown deny, `session` remembered, `always` leaving the shipped template byte-identical plus exactly one changed line, and a closed driver failing the turn. Since Milestone X also the policy: a read-only registry holds nothing that writes (MCP tools included) and the loop advertises exactly that set; `none` dials nothing; an agent's server list connects only what it names and reports an unknown name. |
| `mcp/types_test.cpp` | A response has an id and a notification does not; junk is nothing; namespacing splits on the first `__`; `tools/list` keeps the schema raw and reads a boolean `readOnlyHint` only; `tools/call` joins text blocks and skips the rest. |
| `mcp/transport_test.cpp` | The tail keeps exactly eight lines and tees every byte outside its lock; the transport frames lines across chunk boundaries, normalises CRLF, flushes the last line at EOF, drains stderr into the tail, and fails sends once closed. |
| `mcp/client_test.cpp` | Against the scripted fleet: the handshake wire-for-wire (initialize, the id-less initialized notification, tools/list), tools cached, the image block skipped; a **dying** server failing in under two seconds of a twenty-second bound with its tail on one line; a slow one closed at the bound; junk, unexpected ids and notifications logged and skipped; `isError` as an error the model reads; a cancelled call back at once and an unanswered one at its bound; `close` idempotent. |
| `mcp/registry_test.cpp` | Every personality at once, well inside the bound, never fatal; disabled listed not dialled; progress before every dial and the failure with its tail on one line; tools namespaced, gated unless read-only, dispatching to their server; the direct path `mcp test` takes; the connected event; built-ins identical with zero, one failed, or one connected server. |
| `mcp/serve_stdio_test.cpp` | Three replies for four frames (the notification has none), `-32601` for an unknown method with an id, junk skipped; only a registry's read-only tools served and a writing one unreachable even by name. |
| `scaffold/mcp_server_test.cpp` | The scaffold's files (server executable, test, README), the template complete, the config entry, register-only with arguments, a duplicate refused without force, names sanitised or refused, and `mcp/` in the layout. |
| `scaffold/agent_test.cpp` | The agent scaffold: files beside the config by relative path, the entry's defaults (read-only, `save_subdir` the name), every knob round-tripping, a prose-only agent, duplicates refused without force, bad enums refused before a file is written, a config failure leaving the files and saying so, and the three layout rows. |
| `agentloop/structured_test.cpp` | The validator naming every violation with its pointer, a schema validated as draft-07 (`format` keywords included), the extractor, the instruction block, and the run against the mock: one attempt when conforming with the schema on the request, one correction as durable history, and two misses raw and flagged with never a third request. |
| `agentloop/review_context_test.cpp` | `/branch` parsing (off, a range, a bare head keeping the base), the note naming both refs and telling the model the tools already default, composition. |
| `render/json_report_test.cpp` | `human_summary` last and everything else alphabetical (Markdown and text), nested depth, arrays of objects with inline metadata, booleans and numbers, Title Case from three spellings, raw passthrough and fence stripping. |
| `harness/assets_test.cpp` | The compiled-in prompts and schemas byte-equal to the shipped files; seeding writes absent files only (an edit survives a re-seed, a deleted file comes back); the one seeding path materialises them; an entry overrides a bundled agent case-insensitively and `all_agents` lists it in the bundled slot; relative paths against the data directory; an unedited bundled file is Apogee's. Since Milestone Z also the four kits and the three drivers byte-equal to the shipped files, the compiled-in converter byte-equal to the vendored tree file for file (and larger than one literal could be), `bundled_files` counting every seeded file, seeding materialising them, and a seeded directory Apogee's until something in it is edited (an empty one nobody's). |
| `bundled_agents_test.cpp` | Every bundled schema valid draft-07, closed at the top, `human_summary` the last property in the file and the last required field; every prompt stating the nothing-found rule, ending with the human summary, calling the git tools with no refs, and free of forwarded vendor machinery; a minimal nothing-found report conforming to each. |
| `commands/analyze_test.cpp` | The quiet-save truth table, the filename's shape with no colon, the extension per rendering, and the rendering precedence. |
| `commands/agents_test.cpp` | `agents create|list|delete` in-process: files and an entry that preserves comments and neighbours, a duplicate refused, `--no-schema`, `--purge` and `--keep-files`, a bundled agent with no entry not deletable. |
| `httpserver/admin_knowledge_test.cpp` | The knowledge slice: capture through the route storing the chunk, the archive and the registration with the clerk run once on the served backend as a side request; every refusal with the status a client acts on (`400` for a missing `raw`, a bad status, a bad retriever, a pathed `db`, an unknown or vendor-CLI model, an impossible vector ask — none of them calling the clerk; `501` with no served backend; `502` for a clerk that never conforms) and nothing stored; the finished-record route minting an id and a timestamp, keeping a given id, flipping a superseded record, validating the record; **the parity proof** — a record captured over HTTP and one captured by the CLI from the same conversation are the same record, chunk text and archive included; every knowledge route behind the gate through the mux with the literal paths never read as ids. Since the second slice also the list route (a missing collection an empty list creating nothing, filters, anonymize, the query envelope, `limit`, `501` for vector with no embedder, hybrid reported lexical), get/patch/delete by id (exactly one edit per call, the vector's bytes untouched, honest 404s), `draft: true` storing nothing and warning where a real run would refuse, refine's guards before the clerk and its `501`/`502`, a revision carrying `supersedes` and a dropped source with the clerk shown the refine prompt as a side request, **the round trip** (draft → refine → store field-equivalent to a one-shot capture, the clerk run exactly twice, the footprint unchanged until the store), and reindex (`404`, zero with a note, `501` for vectors with no embedder, the rewrite with one). `?graph=true`: opt-in, the note, the walk, composing with `?anonymize=`. |
| `httpserver/admin_agents_test.cpp` | **The parity proof for agents**: the CLI and the route on two copies of the template leave byte-identical prompt, schema and config; `409` and `force`; every refusal a `400`; the list with the bundled three and an override in its slot; `GET` inlining bodies (compiled-in text, `present: false`, for an unseeded bundled agent); `PUT` in place with the name from the path; `DELETE` keeping files by default and removing them with `?purge=true`; a bundled agent with no entry a `404`. |
| `support/fake_mcp_server.h/.cpp` | The scripted fleet as `Transport`s -- well, chatty, slow, dying, malformed, erroring, slow-calls -- and `fake_fleet`, a `RegistryOptions::spawn` that picks the personality from the spec's `command`. |
| `secrets/store_test.cpp` | Round trip beside the config, `0600` after every write, replace/list/clear, the on-disk shape; a corrupt file reads as empty with a warning naming it, `put` and `clear` refuse to overwrite it, and one bad entry is skipped rather than the whole store. |
| `secrets/resolve_test.cpp` | Which types take a key and their variables in order; the precedence table exhaustively (nothing / env / store over env / config over both / the wrong vendor's slot / a keyless type / `GEMINI_API_KEY` before `GOOGLE_API_KEY`); a snapshot deaf to a later change; the no-key message naming the entry's own variable and `auth add`. |
| `secrets/leak_test.cpp` | **The leak test.** One distinctive key in every rung at once — config, store, environment — and then every surface searched for it: `auth list`, every doctor row and the rendered report, every admin response and the backend view, every build reason, everything the bus published, everything the operational log wrote. |
| `httpserver/admin_auth_routes_test.cpp` | `PUT` refused with `403` from a non-loopback peer, spoofed `X-Forwarded-For` included, before the store is touched; accepted from `127.0.0.1` and `::1` with metadata only; the listing's `data` and `backends` with no key; a vendor-CLI slot and an unknown provider refused with the reason; an empty or missing key `400`; `DELETE` `200`/`404`. |
| `logger/session_test.cpp` | Round trips, every resume-warning path, tool-call survival, and that no thinking is persisted. Since Milestone Y also `transcript_text`: the participants' turns labelled and trimmed, the system prompt, tool results and empty turns left out. |
| `commands/line_reader_test.cpp` | The non-TTY reader: CRLF, a final line with no newline, blanks vs EOF, and that a piped run never constructs the editor. |
| `commands/check_test.cpp` | The doctor against a matrix of deliberately broken installs — and the **severity** each gets, which is the whole product: a keyless, modelless install must PASS. Plus that `--fix` never rewrites config, and that the key itself never reaches the output. Since the credential store: the three key-source wordings (config / the credential store / the variable), a missing key pointing at `auth add` first, and the `Credential store` row — absent is fine, present must be `0600`, `--fix` tightens it, corrupt is a warning naming the file. The `Tools` section: a known key, a misspelt one warned, an `mcp__` key accepted, unlisted defaults, a missing `fs_root`, an unknown toolset in `tools.disabled` — warnings only, and none on the shipped template. Since Milestone X also the `Agents` section: bundled files present or warned with `--fix` as the remedy, a missing file and an unconfigured model failing, a missing collection and server warning, a broken schema failing. Since Milestone Y also the `Knowledge` section: no records and no archive fine, records counted with the index verified, the config's `db` the collection inspected, a world-readable archive failing and `--fix` tightening it. The Graph section: an unconfigured extractor fails, a quiet block has no row, an enabled unbuilt graph warns, a built one reports its shape; every `graphs:` entry -- the collision ban, no members, an unconfigured extractor and a missing member failing, a healthy one reported not yet built with the build as the remedy, then built. Since Milestone Z the `Training` rows: the missing environment a warning with its command, seeded kits and the script ok, the environment's sets reported, an edited script kept and shown, a broken kit a failure, a missing `paths.hf_dir` a warning; since the run item the converter tree (missing, matching, edited), the trainer and `convert set` rows appearing only once the environment exists and naming the set to install, and every ledger -- consistent, a drifted `model_path`, a missing GGUF, an unconfigured backend, an active version the ledger does not hold -- as warnings that never fail the install; since the pipelines item every named pipeline (its student and datasets present, else a warning naming each), the cycle block (incomplete, an unknown pipeline and a non-llamacpp backend failing; a healthy one reported with its sources and breaker) and the history (halted a warning with `cycle resume` as the remedy, idle reported with the anchor and a running lock, unreadable a warning). |
| `commands/lifecycle_test.cpp` | The completion protocol — live backend names, **flags read from the parser** (per-command, short and long, and that a value-taking flag still wins), prefix-not-substring matching, surviving a broken config — and uninstall's plan (which user data it names, `--keep-data`, an already-removed install). Since Milestone X the empty-install case runs over a seeded tree: unedited bundled files are not user data. |
| `knowledge/record_test.cpp` | The record: `normalize` (trim, canonical status and discipline, the default source), the status synonym table, `validate` refusing an empty intent and a non-canonical status, `index_text` never carrying the attribution, the link, the status or the id, `anonymize` keeping every chain field, ids sortable by time and unique within a second, the JSON shape with its omitted-when-empty fields round-tripping, and the decoder refusing empty metadata, non-JSON, an ordinary chunk's JSON, a record under another source, and one with no intent. |
| `knowledge/clerk_test.cpp` | The clerk: the compiled-in prompt and schema byte-equal to the shipped files; the schema valid draft-07, closed, requiring the intent and naming none of the system fields; the system prompt stating the restraint rule and ending with the same OUTPUT FORMAT block an agent gets; `run_capture` handing the clerk the prompt and the raw text; every override winning for its field and only its field, with a synonym folded; a non-conforming answer and a missing intent as failures carrying the validator's words; `finalize`; and the production clerk over the mock — the schema on the request, 0.2, 2048, no tools, a side request, one correction as durable history, two misses a failed capture. |
| `knowledge/store_test.cpp` | The store: the archive written first, `0600` under a `0700` directory, `raw_ref` set, one chunk whose text is the index and whose metadata is the record; a lexical record found by text at once and the name never; re-put idempotent with the archive replaced (an empty raw leaving it alone), archiving off, an id required; `list` newest first skipping plain documents; `set_link`/`set_status`/`supersede` rewriting the metadata with the vector's bytes, the chunk id and the text unchanged; `remove` taking the file; `reindex` rewriting every record or the named ones with the archive untouched, an unknown id refused before any embedding, an embedder failure naming the record, and `vectorless_count`. |
| `knowledge/query_test.cpp` | Records back out: a lexical query with no model, scored and labelled, plain documents never returned; the status filter defaulting to shipped and applied BEFORE the cut (six rejected records that match hardest never starve the one shipped record), every branch on request, discipline on top; `filter_hits` on its own; the resolver's rules on records (an explicit vector ask refused naming lexical, hybrid run and reported lexical, a vector pin excluded with a note); a free embedder with a matching store answering vector and a model mismatch demoting with the note naming both models; the judge reordering by the immutable index text and reporting it, a failing judge leaving the order and saying so, `off` and an unconfigured judge. |
| `knowledge/refine_test.cpp` | The revision pass: the prompt byte-equal to the shipped file and sharing the capture pass's OUTPUT FORMAT block and schema; the user message carrying exactly the six clerk-owned fields, the trimmed instruction, and the raw or an explicit absence; the instruction cap counting codepoints, empty refused; the guards firing before any clerk call (a counting fake); a revision applying the instruction, carrying `supersedes`, keeping a dropped source, coming back id-less; a non-conforming revision a failure. |
| `knowledge/export_test.cpp` | Names off, chain on: the attribution and the local `raw_ref` stripped, `source`/`downstream_link`/`supersedes` kept, the keys absent from the JSON; a faithful export unchanged; the Markdown report grouped shipped, rejected, superseded, other with counts, a bullet per set field, the attribution and decision lines omitted when empty, no archive path anywhere. |
| `embedstore/metadata_test.cpp` | Schema v3 against a **hand-built v2 database** (through SQLite itself — the test target links the amalgamation for this): the column added on open, the old row an ordinary chunk, its vector and the recorded model kept, the old triggers' index still answering, a second open a no-op; metadata stored per chunk, NULL when empty, carried on every hit and never indexed; `update_metadata` leaving the text, the vector and the index alone and clearing to NULL. |
| `embedstore/graph_test.cpp` | Schema v4 (v5 since the fourth slice): the `kg_*` tables and entity index on a fresh and a reopened store, a dropped trigger healed on open; `normalize_entity_name`; `upsert_node` identity, first-non-empty merge, the vector cleared on a text change and kept otherwise; `upsert_edge` corroboration vs `ensure_edge` at weight 1; mention dedup and the salience count; reconcile pruning dead mentions and vanished sources, recounting, dropping orphans and their edges; chunk ids never reused; source states and spans; `delete_graph`; graph meta; the entity index against hostile input; decision metadata JSON. |
| `embedstore/graph_search_test.cpp` | The read side over a hand-built graph: stats (counts, types, coverage, staleness by count, max id, model, legacy rows), exact and fuzzy lookup, `nodes_by_ids`, neighbourhoods by relation then weight, capped chunks, and `graph_expand` -- one and two hops, the clamp, the cap, weight × mentions ranking, hop-0 seed nodes listed and chunk seeds not, empty seeds. |
| `graph/extract_test.cpp` | The prompt and schema byte-equal to the shipped files, closed over eight types with no reserved one, the system prompt ending in the one OUTPUT FORMAT block; `normalize` (an unknown type dropped with its relations, an extractor-emitted `decision` dropped, empty names, duplicates merged, self-loops, unresolved endpoints, duplicate relations, the caps); tolerant decoding; the production extractor as one structured call with the schema, a side request, the temperature and cap, one retry, and an unroutable backend a failed outcome. |
| `graph/build_test.cpp` | The build against fakes: populate and stamp; a dry run committing nothing; the resume rule (a limit stops mid-file, only that file re-extracts, a third build is a no-op, `--force`); a model change; **the fingerprint** -- a same-count re-ingest re-extracts and the entity returns, a legacy row compares the count only; a failed chunk retried once then counted with its file unstamped; cancellation between chunks; the embed phase last over mutated nodes only, a soft failure keeping the extraction and recording no model. |
| `embedstore/graph_communities_test.cpp` | Community storage: the row, membership and pseudo-chunk written together and found by plain search; the summary never corpus (planning, coverage, staleness); the vector arriving later and reachable by cosine; replacement by key, the prune sweep, `delete_graph` taking them and keeping the real chunks. |
| `embedstore/graph_dedupe_test.cpp` | Dedupe: a dry run writing nothing; the earliest node surviving with edges repointed and folded to a summed weight, self-loops dropped, mentions unioned and recounted, the description merged and the vector cleared, the entity index following; namesakes of different types never merging, the threshold respected, unvectorised nodes skipped; decision nodes never merged beside an entity pair that is, their memberships dropped; a second run a no-op. |
| `embedstore/graph_multi_test.cpp` | The provenance columns in use: labelled mentions and state rows, `reconcile_graph_multi` per member (a dead chunk, a vanished source, an ex-member, a null member), the named graph's identity in `graph_meta` and `delete_graph` clearing it, `graph_stats_multi` per member with a missing one flagged, `node_mention_refs`, `chunk_ids_existing`, and labelled expansion seeds -- the same chunk id under the wrong label seeding nothing. |
| `graph/communities_test.cpp` | The prompt byte-equal to the shipped file; the detector (two clusters largest first, the min-size filter, weighted-majority adoption, determinism, edgeless nodes never joining); the key and the rendered text with its relation cap; `build_communities` populating once, skipping an unchanged membership, regenerating on `force`, pruning and re-summarising on a membership change, a summariser failure leaving the old summary, the embed phase last and healing an earlier failure on the next run, cancellation between communities. |
| `graph/graph_multi_test.cpp` | The build over members: one node per entity across two collections with labelled mentions, weight accumulated across members, per-collection state, the identity stamped, a no-op second build; a member dropped from the list converging; the limit stopping across members and the next build resuming; a null member contributing nothing; a record in one member a decision node reachable from another's chunk; a dry run stamping nothing. |
| `agentloop/graph_named_test.cpp` | The precedence rule: `graph_db_path` under `graphs/`; `named_graph_covering` honouring built-only, config order and case; `resolve_turn_graph` preferring a built named graph (its knobs, the member's label) over the collection's own block over nothing; **the decoy tripwire** -- a turn through a built named graph renders the graph's section and never the member's own decoy, and the member's own graph takes over the moment the database goes; labelled seeding of a graph store. |
| `graph/records_test.cpp` | Records seeded through `knowledge::Store` so the chunk shape is production's: the description clip by codepoint; decision nodes with `concerns` and `supersedes` edges, a missing target skipped with no placeholder, a docs chunk reaching the decision in one hop; an idempotent rebuild with weight-1 edges and the status refreshed without a re-embed; a deleted record retired by reconcile and a re-put one re-materialised on its new chunk; an extractor-emitted `decision` dropped; a dry run counting only; description-only embedding. |
| `agentloop/graph_context_test.cpp` | `entity_line` plain and with the branch marker; **the acceptance fixture** -- two chunks sharing no vocabulary, linked only by an edge: a lexical query matching A injects B's entity; the query's own terms seeding with no chunks at all; the budget cutting whole lines and never leaving a triple without its entity. `agentloop/rag_test.cpp` gained the turn's half: an enabled graph expanding the chunks, riding the transient prefix and counted; the section seeded BEFORE the judge, surviving a verdict that keeps nothing; a vector turn seeding from its chunks alone; a collection with no graph quiet. |
| `commands/graph_test.cpp` | `apogee graph` on the scripted mock: a build stamping the sources and appending the graph block to the config byte-exactly, a second build a no-op, stats, show (exact and closest), delete keeping the chunks; `--dry-run` storing nothing; the cost policy (a metered default refused naming the three ways, through the collection's block or the extraction role allowed, a vendor CLI and an unconfigured name refused, a missing collection refused); a clerk that never conforms counting failed chunks with the file unstamped and the next build retrying it. Since the fourth slice also `config add-graph`/`delete-graph` (every refusal by name, an unknown member a warning, the byte-exact entry, `--force`, delete leaving the database); a named graph built into its own database over its members with nothing enabled, per-member stats, `show` naming each chunk's collection, retrieval precedence through `retrieve_for_collection`, the drift note and a converging rebuild, `delete` removing the file; `communities` refusing a relation-less graph, summarising once at `--min-size 2`, listing, skipping an unchanged membership, the summary the top lexical hit, the metered default refused for the summariser too; `dedupe` previewing, merging and idempotent; `embed ingest --graph` building the covering graph after a successful ingest and never after a failed one. |
| `httpserver/admin_graph_test.cpp` | The graph slice: the build as a job on the plane's harness with its counts and the enabled write landing byte-identically, a second build a no-op, force and limit; the refusals (404, 400s, a metered default, 501); a failing extractor counted; cancel wins; stats never an error; entity exact then fuzzy; delete; the `PUT` twin byte-identical to the editor's own edit; every route behind the gate through the mux with `{id}` mid-path. Since the fourth slice also the named routes (an unbuilt graph's stats zeros with its shape, a build over the members as a job with nothing enabled, `members[]`, an entity's chunks by collection, delete removing the file), `communities` as a job with its list and refusals (`400` for a metered default, `501` unserved), `dedupe` synchronous with `dry_run`, and the new routes through the mux. |
| `httpserver/admin_graphs_test.cpp` | The `graphs:` slice and its parity proof: an entry made over HTTP byte-identical to one made by the CLI, `built` following the database, the CLI's rules as `400`s (the collision ban for a registered and an on-disk collection), `409` on a duplicate, `PUT` replacing in place with a mismatched body name refused, `DELETE` leaving the database. |
| `training/script_runner_test.cpp` | Every line shape classified; events in order and the run ok on a clean exit; the stream framed across 3-byte chunks; an `{"error"}` line the outcome's error; a bad exit named with its code and the stderr tail; a spawn failure naming the script; cancellation terminating the child; the command formed from interpreter, script and arguments; `run_command` capturing both streams; `tail_of`. |
| `training/python_env_test.cpp` | Against a recording runner that fakes the interpreter: the sets named with packages; create running `-m venv` with the directory and recording the origin, a second create a no-op; install running the environment's own pip with the set's floors and recording the set once; a failing command named with its exit code and stderr; install before create naming the setup command; a configured interpreter that is missing refused by name, an existing one used as given. |
| `training/kit_test.cpp` | The four bundled kits with their numbers and every eval item carrying an `expected`; defaults filling in; validation's refusals; a malformed kit naming what is wrong; listing sorted with a broken file named, find by name or path, the stem naming a nameless kit; the eval suite one object per line, prompt before expected. |
| `training/synth_test.cpp` | The contract appended and the batch instruction; array extraction through prose, fences and brackets inside strings; the aliases with blank halves dropped; the call bound; duplicates dropped and an under-producing teacher stopping at the bound; the batch asking for the remaining count and cycling the seeds; a retryable failure retried with backoff (doubled, then capped) and a final one not; a throwing teacher a failed batch; cancellation ending the run with nothing merged past the stop; parallel batches reaching the target, and really running at once (a call that waits for a second in flight); no teacher an error. |
| `training/datasets_test.cpp` | Plain names; a chat line with role before content; the store writing atomically, refusing an existing dataset without force, listing with shapes, removing, never leaving a temp file; sessions mined one completed exchange at a time -- a blank side skipped, a tool-only turn waiting for its answer, an unanswered question counted -- with the backend and date filters and the date validation. |
| `training/prepare_script_test.cpp` | The shipped script under the host's `python3` (skipped by name without one): every standard-library preset, auto-detection, `--map`, `--as-eval`, `--flat`, the no-match refusal listing the columns and a `--map`, a JSON object of splits and a missing split naming the rest, a directory read whole, the terminal record; and the static rule -- the environment guards precede the first ML import. |
| `commands/datasets_test.cpp` | `apogee datasets` on the scripted mock: create from the template (the bytes), a duplicate refused, `--force`, `--from empty`, a bad source; `--from sessions` with the filters and an invalid date refused before writing; synth through a named teacher de-duplicating and stopping at the bound, an existing dataset refused, `--force`; the refusals -- an unknown teacher, a vendor CLI, an unknown kit, a kit that cannot gate, a teacher that never produces (nothing written) -- and a metered teacher accepted because it was named; kits, list, info, delete with and without `-y`; `prepare` refusing on a pipe naming `apogee train setup`; the teacher resolution and the parallel rule. |
| `commands/train_test.cpp` | `train setup` refusing a configured interpreter that does not exist, an unknown requirement set and a bad trainer name, each before anything is created; the trainer detection naming its reason. Since the run item, on the mock through the real command tree: `run` writing a running manifest that ends complete with the status line seen and the dataset hashed, the student and dataset by path, the config's trainer; every refusal before a run directory exists (a GGUF, a backend name, a missing snapshot, an unknown dataset, a bad method, an unknown trainer, a real trainer on a pipe naming setup); the resolvers (a student by directory or name under either root, a dataset by name or path, a suite by path, under `suites/`, as a prepared eval or as a kit) and the iteration rendering; `eval` skipping loudly without a judge, judging pairwise through the scripted mock, failing the gate on a `B`, taking the judge from the config, refusing a vendor CLI and an unknown backend, re-running only with `--force`; `promote` refusing before anything is built (no eval, a non-llamacpp `--as`, a bad name, `--quantize` in a build without llama.cpp, an unknown type), registering a new entry byte-exactly, repointing an existing one with only the path changed, `--keep-fused`, hard/soft/`--force`, retention pruning under `retain_versions: 1` and the next version being `max + 1`; `rollback` repointing at the previous version and deleting nothing, the oldest version having nothing below it, a removed backend refused; `versions` and `status`; a failing driver a failed run (exit 2, the manifest `failed` with the error, neither evaluable nor promotable) and a promote whose fuse fails leaving the config byte-identical with no ledger and no fused tree. Since the pipelines item: `pipeline run` over a spec file chaining fused checkpoints, aborting on a regression (exit 2, the stage runs carrying their lineage), `status` printing the table and the resume hint, `resume` after the fix completing with stage 0 untouched, the stage run evaluated and promoted like any other, a complete run refused, a named pipeline from the config; every refusal before a run directory exists; judge items through a named judge and a vendor CLI refused. `regime run` distilling through the mock teacher over the kits in the given order with `--no-promote`, promoting with `--as` (the ledger and the config written), a named regime with flags winning, a spec file; every refusal first. `cycle run` skipping with nothing queued, passing and promoting into the config's backend (the entry in the backends section, not a line removed), setting the anchor, consuming the queue; a regression failing, discarding, and tripping the breaker at 1; halted refused until `resume`; `halt` twice; `--source`; an unconfigured cycle refused naming the keys; the sessions source refused at load without consent, and with it consumed once through the watermark; `status` rolling up the cycle. |
| `training/trainer_test.cpp` | The selection rule on every host shape and an unknown name refused; a student validated (a GGUF and a non-snapshot refused naming `--safetensors`); the mock emitting exactly `iters` events with the total and a falling loss and writing a token adapter, the request's count winning, cancellation mid-stream leaving no adapter, an error surfacing, the capabilities; the mock's fuse, its echoing candidate with the base distinguishable, and its GGUF parsing under the header reader; the mock scripted by the dataset's first line -- an error, an iteration count, a fuse failure a fresh mock reads from the adapter -- and an ordinary dataset scripting nothing; the scripted `answer` the trained candidate replies with (the untuned base still echoing), carried into a fused checkpoint and inherited by a stage trained from it. |
| `training/progress_contract_test.cpp` | The JSONL progress protocol on BOTH sides. C++: the golden fixture parsed event by event; the script trainer delivering the fixture's events over a scripted child with the final loss carried, an error line making the run not ok with the stderr tail, a clean exit ok, a bad exit named with its code, cancellation; the argv each mode sends, pinned; infer reading the `{"text"}` record and a driver that prints none an error; the bytecode guard on every run. Python, on the host's bare python3 (skipped by name without one): `train_mlx.py` under the stub `mlx_lm` laying out the `{train,valid}.jsonl` directory, forwarding every flag including `--mask-prompt`, parsing the newer no-total line shape, a failing `mlx_lm.lora` an error with the exit code, an empty dataset refused, fuse and infer (the chat template applied, the untuned base sent as no adapter), the install hint without the package; `train_peft.py` under stub `transformers`/`peft`/`torch` with `APOGEE_TRAINING_STUB_MODULES=1` -- the exact prompt mask in the recorded batch, QLoRA's 4-bit load, gradient checkpointing, fuse through `merge_and_unload`, infer decoding only the new tokens, the missing-deps hint -- and on Apple Silicon its hard-exit as the first and only line; the guard-before-import rule on both drivers by grep. |
| `training/manifest_test.cpp` · `training/store_test.cpp` | The manifest round trip with status, error and eval, and a stage run's lineage absent from a plain run; atomic write and read with every failure named; run ids as UTC timestamps suffixed on collision and never path-shaped, with a prefix; the dataset digest. The store: runs newest first with an unreadable manifest skipped and a running one shown, an unknown or path-shaped id nothing, the summary JSON; the ledger round trip (pruned entries, optional eval fields, sorted on read), atomic save, every ledger by backend with a broken one skipped; pipelines newest first with a broken manifest skipped, one by id, the running one active, the summary JSON. |
| `training/eval_test.cpp` | Substring items on the exact text and the 100% gate; judge items pairwise with the first-word verdict (`A`, `b`, `TIE`, prose → tie) and the candidate always Response A; the never-fail contract (a judge error and a baseline error each a tie that passes, a judge without a baseline a skip); a candidate error aborting, cancellation, an empty suite; the mock's candidate and base through the gate; the suite loader's rules; the JSON round trip. |
| `training/promote_test.cpp` | The gate under hard, soft and `--force`, an incomplete run always refused; `max + 1` after a prune (the reference's regression pinned) and the plan's paths; the artifacts -- fuse once, the converter with the fused tree and the version path, the fused tree dropped unless kept, a taken version path refused; a failure at any step leaving nothing behind (fuse, convert, an unparseable GGUF, the quantizer, cancellation) and the quantized path removing the F16; retention never pruning the active version, marking rather than erasing, the history keeping numbers taken; the rollback target across gaps, and no history, one version, a pruned target and a missing file refused by name. |
| `httpserver/admin_training_test.cpp` | The training reads: status with the running ids, the ledgers, the active pipeline, the lock state and the cycle's headline; the runs list -- runs and pipeline runs tagged by kind, newest first, `data` never null, the kind filter and a bad kind `400`; one run's or one pipeline's manifest, a `404`, a `400` for a path-shaped id; versions by backend or all; the cycle route with `active` and its `404`; the routes gated through the mux, a query filter through it, and nothing under `training/` -- the cycle's control paths included -- answering a mutating request. |
| `training/pipeline_test.cpp` | On the mock trainer: stage 0's base the snapshot and stage 1's its fused checkpoint, only the intermediate stage fused, the stage runs carrying `parent_run`/`pipeline_run_id` and the cumulative suite; a stage regressing on an earlier suite failing the cumulative gate -- the run `aborted`, the stage `failed`, later stages `pending` -- and resume running only the unpassed stages to `complete`, a complete run and a drifted spec refused; soft mode and `--continue-on-fail` completing with mixed statuses, the failed stage still fused and the regression persisting through it; rehearsal mixing deterministic across runs and recorded as what trained; the manifest rewritten on every transition; cancellation leaving `aborted` with the stage's run `cancelled`; judge items through the closure pairwise against the untuned base; the request's refusals, `merge_suites`, the JSON round trip. |
| `training/regime_test.cpp` | Flags over a loaded spec field by field, `--all-kits` alphabetical with an explicit `--kit` list winning; the kit validation refusals; the per-kit stage defaults with the regime's `iters` overriding; a run on a scripted teacher -- a dataset and a materialised suite per kit, one stage per kit with the kit's defaults, the last passing stage named for promote; a teacher producing nothing failing by kit name, the refusals before the work directory exists, cancellation. |
| `training/cycle_test.cpp` | The history round trip, loaded empty for a backend, saved atomically, a bad file named; the lock exclusive, released on destruction and by hand, re-acquirable, moved; the breaker under `k`, halted, at `k`, disabled at 0, halt and resume, `trip_breaker`; the directory source ignoring non-jsonl files, creating a missing queue, empty cleanly, consumed files moved aside; the sessions source refused without consent naming the key and the two risks, the watermark, the backend and since filters, a blank side skipped; the merge skipping blank lines; the six anchor-gate cases, the last passing score, the counters; one pass end to end -- skipped without a failure counted, a pass promoting the final stage, consuming, setting the anchor and resetting the count, a regression failing and halting at `k`, halted and locked refused; a failed promotion counted, `--source`, a pinned `anchor_version`, the sessions source consumed once through the watermark and refused without consent. |
| `httpserver/admin_datasets_test.cpp` | The datasets slice: **the parity proof** -- a dataset created over HTTP byte-identical to the CLI's, and a synth over HTTP byte-identical to the CLI's from the same teacher; list, get, `409` on a duplicate, `force`; create's `400`s and explicit lines; honest `404`s; the kits route; synth as a job (`202`, the record, the file) and a teacher that produces nothing failing the job with nothing written; every CLI refusal as a `400`, `409` for an existing dataset, `501` with no served backend; the routes gated through the mux with `kits` and `synth` never read as dataset names. |
| `support/fake_transport.h/.cpp` | The scripted `HttpTransport` — the seam that makes cloud-backend tests hermetic. |
| `support/fake_command.h/.cpp` | A `Command` defined in test code — the injectable seam, exercised. |
| `support/env_guard.h/.cpp` | RAII environment-variable and temp-directory guards. Config resolution reads the environment, so exercising it means mutating the environment — and a leaked change would steer every test after it. |

Beyond those, `tests/CMakeLists.txt` registers `cli.*` ctest cases that run the built `apogee` binary as a subprocess, covering the contract as a user meets it (bare invocation prints help and exits 0; `--version`; unknown subcommand fails). Running a target is not linking it, so the link policy still holds.

`cli.install_parity` (`tests/install_parity.sh`) installs twice into throwaway roots and requires identical trees *and modes*, refuses to pass on fewer than five directories, and requires a freshly seeded install to pass `apogee check`. It is the enforcement behind CLAUDE.md → *One layout declaration*.

`cli.test_names` (`tests/test_names.cmake`) refuses a `TEST_CASE` name that will not survive being handed to Catch2 as a filter argument — which is what ctest does with every test's name. Two shapes break. A **leading dash** is parsed as an option: the test passes alone and fails under ctest with "Unrecognised token", which reads like a broken test rather than a broken name, and cost time twice before this existed. An **embedded double quote** is worse, because it fails silently — it breaks the generated command's shell quoting, the filter splits in two, neither half matches, and Catch2 prints "No tests ran" and **exits 0**, so the entry passes forever without executing. That case was found in Milestone L when a mutation broke exactly what a test asserted and the suite stayed green.

`cli.one_role_resolver` (`tests/one_role_resolver.cmake`) fails any source outside the resolver, the config engine, and the two commands that must print the raw value if it reads `models.default_*` directly. That line is how a second resolution chain starts — never written deliberately, always as one innocent-looking ternary inside whatever command needs a backend name, which is exactly how Ommi's CLI and admin plane came to disagree. The table test proves the chain is correct; this proves it is the only one, and a correct resolver nobody calls fixes nothing. It refuses to run if the allowlist swallows every file.

`cli.one_key_resolver` (`tests/one_key_resolver.cmake`) is the same guard for secrets: it fails any source outside `secrets/resolve.cpp` and the allow-listed readers (the config engine and its editor, which parse and write the field; `config add-backend`, which stores the flag literally; `admin_config`, which reports `api_key_set`; `auth`, which tells the user which variable to export) that reads an entry's `api_key` or names a conventional variable. A CLI and a server using different keys for the same entry is a bug nobody notices until a bill arrives, and it starts with one inline `entry.api_key.empty() ? getenv(...) : ...`. Comments are exempt; it refuses to run if the allowlist swallows every file.

`cli.no_vendor_credentials` (`tests/no_vendor_credentials.cmake`) greps every source for signs of reading a vendor CLI's credential store — their dot-directories (`.claude/`, `.gemini/`, `.codex/`), `credentials.json`, `google_accounts.json`, `oauth_creds.json`, `apiKeyHelper`, OAuth, the keychain. `~/.ollama/` is deliberately **not** listed, and the file says why: that directory is a *model* store, and reading it is a supported source (shipped — Milestone N). It enforces the SPEC principle *a vendor CLI is spawned, never opened*, and it is mechanical rather than reviewed because the shortcut it forbids is genuinely tempting: when a `--resume` fails, reading the vendor's session file *would* recover the conversation. Documentation lines are exempt so the rule can be explained where it is enforced. It has exactly one exemption, with its reason in the file: Apogee's own store is `config/credentials.json`, and the single line that names it — the constant in `secrets/store.h` — is skipped for that one pattern; every other file, `secrets/store.cpp` included, is still held to it.

`cli.auto_rag` (`tests/auto_rag_e2e.sh`) covers the two things about `auto_rag` only a real shell can show: `--rag ""` switches it off for one run on both surfaces — CMake's `execute_process` drops an empty list element, so the `cmake -P` lifecycle test cannot pass an empty argument at all, and `--rag=` reaches CLI11 as a flag still awaiting its value, which swallows the prompt and blocks on stdin — and the key is **re-read at turn build**: two questions fed to one `chat` with the key switched off in the file between them announce `auto_rag` exactly once. POSIX only, a recorded skip like the other shell checks.

`cli.machine_mode` (`tests/machine_mode_e2e.sh`) drives the real binary over real pipes: every stdout line must open with `{`, one child must serve two turns without respawning, a contradictory `--input-format`/`--output-format` pair must be refused rather than half-honoured, and `lsof` is sampled continuously while the child lives to prove machine mode opens no listening socket. Those are properties of the *process*, not of a class, so no unit test can reach them.

`cli.reference_driver` (`tests/reference_driver.py`) is the worked example a GUI author reads *and* its test-lock. It reconstructs the conversation from the JSONL and requires three independent paths to agree: the concatenated `answer_delta` chunks, the `result` event's text, and what `apogee complete` printed in text mode. Disagreement means one surface grew a behaviour the other lacks — the failure the shared Reporter seam exists to prevent.

`cli.machine_schema_conformance` (`tests/schema_conformance.py`) pins [`machine-mode.md`](../reference/machine-mode.md) to `json_reporter.cpp` in both directions: an event the emitter produces but the document never describes, or one the document promises but nothing emits, fails the build. A protocol document that drifts is worse than none — a front-end author trusts it, builds against it, and debugs Apogee for a fault that is in the prose.

`harness.layering` is a `cmake -P` check that no file under `source/harness/`, `source/agentloop/`, `source/agent/`, `source/secrets/`, `source/tools/`, `source/mcp/` or `source/knowledge/` includes `backends/` (with one named allowance for `mcp/`: the JSONL framer; and, since Milestone Y, an allow-list for `knowledge/` — `embedstore/`, `agentloop/`, `agent/`, `harness/`, `platform/` and itself, never a surface) — and, since Milestone U, that `source/events/` includes nothing from the project at all, because a bus anything can publish to must be a leaf. The credential store added a third shape: `source/secrets/` may include `harness/` (for the backend types) and itself, and nothing else — the one place that returns a key must not depend on a surface that renders. C++ cannot enforce this the way Go's import cycles do — the include would compile fine and the layering would be silently gone — so it is checked mechanically, and it refuses to run against an empty source list so it cannot pass vacuously.

`cli.complete_lifecycle` ([`tests/complete_e2e.cmake`](../../src/cli/tests/complete_e2e.cmake)) runs the walking skeleton end to end against the mock backend — prompts, stdin, flags, images, `--all-backends`, and every exit code — fully offline. **Every invocation gets an explicit stdin**: without one the child inherits ctest's, which may be a pipe that never delivers EOF, and a command falling through to reading stdin hangs the whole suite instead of failing. Since Milestone V it also runs `check --fix` and reads the `Tools` section, round-trips `config set-permission` through `config get`, refuses a level that is not one of the three, and runs `--tools` with the native set registered.

`cli.startup_speaks_on_one_line` ([`tests/pty_startup_check.py`](../../src/cli/tests/pty_startup_check.py)) drives the real binary under a **pseudo-terminal**. A pipe-based test proves nothing about the interactive path — `apogee` asks whether stdout is a terminal before rendering anything, so piping exercises the branch that deliberately emits nothing, and would pass on a build that rendered garbage interactively. The script replays the escape codes to reconstruct the final screen, then asserts the startup notice appears exactly once on one row and that no spinner frame survived. POSIX only.

`cli.chat_line_editing` ([`tests/pty_lineedit_check.py`](../../src/cli/tests/pty_lineedit_check.py)) drives the editor under a PTY: an arrow key must RECALL previous input rather than arrive as a literal escape sequence in the message, and the history file must persist. None of it reproduces on a pipe, which is the point.

`cli.chat_typeahead_and_crash_safety` ([`tests/pty_chat_check.py`](../../src/cli/tests/pty_chat_check.py)) covers the two chat behaviours no unit test can reach: text typed **before** the first prompt is discarded (needs a real terminal — `tcflush` applies to a terminal input queue, and on a pipe the code deliberately does nothing), and a `kill -9` mid-conversation leaves every completed turn on disk (needs a real SIGKILL). Both were verified against the mutation each exists to catch.

The interactive-never-listens invariant has two locks, `cli.no_listen_symbols` and `cli.complete_opens_no_listening_socket`. Read [CLAUDE.md](CLAUDE.md) → *⚠ Interactive turns never open a listening socket* before touching either — the reason there are two is not obvious, and removing one leaves a real gap. Since Milestone T the symbol check scans **every library the executable links that this build produces — static archives and shared libraries alike**, collected by walking the link graph in `tests/CMakeLists.txt`, attributes each `listen`/`accept` reference to its object file (or, for a shared library, its file), and allow-lists exactly one: `serve.cpp.o`. Shared libraries matter because llama.cpp is one under the llama preset, and a shared library's imports never show in the executable's own symbol table — the earlier binary scan was blind to it. It refuses an empty list and requires the allow-listed object to be *seen*, so it cannot pass vacuously.

`cli.serve_lifecycle` (`tests/serve_e2e.sh`) is the positive half of that boundary and the process-level check on `apogee serve`: a non-loopback bind without `--allow-remote` is refused on the real command line, the server on `--port 0` reports its port and `lsof` shows it listening, a session minted over HTTP is continued over HTTP and then resumed from the terminal with `chat --resume`, a streamed body reaches curl whole and ends with `[DONE]`, a vendor-CLI entry is refused with the reason, an unknown route is a JSON 404, and SIGTERM stops the server with exit 0. POSIX only, like the other shell checks.

`cli.serve_lifecycle` also drives the control plane on the real binary: `--print-admin-token`, the token file `0600`, `401` without a bearer, with a wrong one, and with the right one in the query string; an add-backend over HTTP that `config get` reads back and that leaves the **same bytes** the CLI's add-backend does; and a session minted on the public plane arriving as `session.created` on the event stream, which never carries the token. Since the credential store: a key stored with `auth add --stdin` lands in a `0600` `credentials.json`, `auth list` shows the slot, `check` attributes a keyless `openai` entry to the store, `GET /v1/admin/auth` returns its metadata (`401` without the bearer), a `PUT` from loopback is accepted and a vendor-CLI slot refused, `DELETE` clears and then `404`s — and the secret is then grepped for in every file the run wrote and every response it received, and found only in the store.

`cli.mcp_lifecycle` (`tests/mcp_e2e.sh`) drives the MCP client on the real binary: `mcp create` scaffolds a server whose own `test_server.py` passes, `mcp test` echoes through the production client with `connecting:` printed before the dial and invalid JSON refused, `mcp list` shows the live state and the negotiated protocol, the in-binary `__mcp-tools` is registered as one more server and answers a read-only tool while a writing one is unreachable, a `complete --tools` run connects at startup and reports a dying server with its stderr tail folded in exactly once, and `disable`/`enable`/`delete-mcp-server` round-trip through `config get`. Skips itself without python3.

`cli.analyze_lifecycle` (`tests/analyze_e2e.sh`) drives `analyze` on the real binary against a fixture repository with a branch that is not checked out, on scripted mock backends: `--list` with no backend; `--branch feature` reviewing `main...feature` from the flags while the input text names another branch, the branch left un-checked-out, the report saved under `analyses/security-review/` with no colon in its name and its rendered form ending in `## Human Summary`; an ad-hoc `--prompt` run printed and not saved; the validator's retry delivering the corrected answer and two misses delivered raw with `"conforms": false` and a stderr warning; a `claude-cli` backend refused naming the type; `agents create` runnable at once and visible to the doctor, `delete --purge` clean; and `chat --tools --branch` reviewing the same diff, the note visible to an echoing mock on the turn and gone on a resume without the flag. Skips itself without git.

`cli.knowledge_lifecycle` (`tests/knowledge_e2e.sh`) drives the knowledge layer on the real binary with the scripted mock as the clerk: `check --fix` seeding the `knowledge/` row; a dry run printing the draft and the store decision with the collection absent, the archive absent and the config byte-identical afterwards, `kn` as the alias; a capture stored, registered under `embeddings:`, archived under a `0700` directory, findable by `embed query` by its reasoning and never by the attribution; every override winning and `--supersedes` recorded, a missing target a note; the doctor counting the records and the private conversations; a vendor CLI refused by type and a clerk that never conforms storing nothing; `/capture rejected` in a piped chat and `capture --from-chat` over that session with the archived transcript the session's own words. Since the second slice the script runs the lifecycle too: `query` on the default branch (the shipped record, never the rejected one), on `--status rejected` and on every branch with the retriever named in the JSON envelope, an explicit vector ask refused naming lexical; `list` counting five; `info --raw` printing the archive; `link` and `status` landing in `info --json`; an anonymized export with no name and no archive path but the chain kept, a Markdown report to a file; `reindex` honest about a lexical collection; `delete` taking the archive and the doctor counting one fewer.

`cli.graph_lifecycle` (`tests/graph_e2e.sh`) drives the knowledge graph on the real binary with the scripted mock as extractor: the cost policy refusing a metered default before any provider call; `graph build --dry-run` printing every extraction with zero footprint; the build stamping both sources and writing `graph.enabled` with every byte above the entry untouched; `stats` and `show`; a second build a no-op; a `complete --rag` turn whose status line counts the graph entities while the answer carries the entity of the document the query never matched and not that document's text; an entity-name query expanding with no lexical match; a captured knowledge record materialised as a `decision` node with its `concerns` edge and shown with its markers; `knowledge query --graph` and its JSON; a docs turn reaching the decision; then the global layer -- `communities` on the scripted summariser producing a summary that is the top lexical hit for "main themes", an immediate re-run reporting it unchanged, the summary chunk never counted as a graph node; a named graph over `notes` and `knowledge` refused a collection's name, added, reported by `check`, built into its own database with both records materialised and progress naming the member, `stats` with the per-member breakdown, `show` naming each chunk's collection, a notes-only turn expanding under the graph's header and reaching the decision captured into the *other* collection, `dedupe --dry-run` finding nothing, `config delete-graph` leaving the database and the collection's own graph resuming, `graph delete` removing the file; `delete` clearing the graph and keeping the chunks.

`cli.datasets_lifecycle` (`tests/datasets_e2e.sh`) drives the datasets floor on the real binary: the four seeded kits listed and the deferred tool kit absent, a template dataset created, a dataset distilled from the scripted mock as a named teacher and a vendor-CLI teacher refused with nothing written, `datasets prepare` refusing on a pipe before the environment exists and naming `apogee train setup`, `train setup` creating the environment from the host's python3 (installing nothing) and `check` reporting it, `prepare` converting a JSONL file through the seeded script under the environment's interpreter, then list, info and delete. It exits 77 -- ctest's skip -- on a host with no python3, since the environment half cannot run without one. POSIX only, a recorded skip like the other shell checks.

`cli.train_lifecycle` (`tests/train_e2e.sh`) drives the run on the real binary with `--trainer mock` and no Python at all: a template dataset, `train run` writing a manifest with the status line seen, a scripted driver failure recorded as a failed run and shown by `status`, a GGUF and a backend name refused naming `--safetensors`, a promote before any eval refused with nothing written, `train eval --suite` with substring items passing against the mock's echo and the judge-less item skipping loudly, a kit's inline suite by name, `promote --as` registering a new `llamacpp` entry byte-exactly (the diff is the entry's lines and nothing removed) and writing `v1.gguf` that `models info` inspects, a second promote repointing the same entry with only the path changed, `rollback` to v1 as the exact inverse deleting nothing and a second rollback refused, `retain_versions: 1` pruning v1 and v2 while the next version is `v3` (never the count), the pruned entry shown as history and a rollback into it refused, `status`, and `check` green with the ledger and converter rows. Then the pipelines item on the same binary: a two-stage pipeline whose second stage regresses the first's suite aborting under the cumulative gate (stage 0 fused, the last stage not, the lineage in the stage manifests), `pipeline status` with the resume hint, `resume` after the data is fixed completing and the last stage promoted like any run, a complete run refused; a regime over two kits with the mock backend as the teacher and `--no-promote` (a dataset and a suite per kit, one pipeline, no new ledger); and the cycle from a queue directory -- skipped with nothing queued, a pass promoting into `training.cycle.backend` with the anchor set and the file consumed and not a config line removed, a regression failing, discarding and tripping the breaker at `k=1`, the halted loop refused naming `cycle resume` and reported by `check`, resumed and running again with the lock released, `train status` rolling both up, `check` green.

`cli.mcp_server_stderr_stays_off_the_terminal` (`tests/pty_mcp_check.py`) is Ommi's Milestone P postmortem locked under a real pseudo-terminal: a noisy, dying server's stderr lines appear nowhere on the rendered screen, every row is Apogee's own, exactly one connect-failed row carries the server's last line, and startup ends well inside the connect timeout.

`cli.http_api_conformance` (`tests/http_api_conformance.cmake`) pins [`http-api.md`](../reference/http-api.md) to the route table in `httpserver/mux.cpp` in both directions, the way `cli.machine_schema_conformance` pins the JSONL protocol: a served route without a heading, or a heading without a route, fails the build.

The largest of those is `cli.config_lifecycle`, driven by [`tests/config_e2e.cmake`](../../src/cli/tests/config_e2e.cmake): it runs the real binary through init → ingest (asserting the file is **pristine bytes plus exactly the one new `embeddings:` entry**, and that a re-ingest reads the entry's chunking back and complains about nothing) → add-backend → roles → `auto_rag` on a real `complete` and a piped `chat` (announced, flag-overridable, and absent from the saved session) → get → delete, and asserts the file came back byte-identical, all under a throwaway `APOGEE_HOME`. It is a `cmake -P` script rather than a shell script so it runs on all six targets — a `.sh` would silently skip on the Windows runners, which is exactly where a path or line-ending bug would surface.

**Tests must stay hermetic:** no network, no models, no writes outside the test's own temp directory. A test that needs a live provider is not a unit test. Fixtures go through the injectable seams — that discipline starts at the first interface because retrofitting it in C++ is far more painful than in Go.

Two traps worth knowing:

- **Test names must not begin with `-`.** ctest invokes each case by passing its name as an argument, and Catch2's own parser would read it as a flag.
- **`#` is not a comment inside `.clang-tidy`'s `Checks:` block.** It is a YAML folded scalar; a `#` line silently becomes a bogus check name and breaks every entry after it. Rationale goes above the block.

---

## Build system

CMake ≥ 3.25, C++20, one static library plus one executable. Build root: `lib/src/cli/CMakeLists.txt`.

### Presets

Six configure presets: `default` (host native — the developer bootstrap) plus one named after each release target. Each builds into `lib/src/cli/build/<preset>/`, which is the path `cicd.sh` and the Makefile expect.

```
linux-x64   linux-arm64   macos-arm64   windows-x64   windows-arm64
```

The macOS preset pins `CMAKE_OSX_ARCHITECTURES` to `arm64`; there is no Intel Mac target (dropped 2026-09-19). On a Mac the build root resolves the SDK through xcrun (`CMAKE_OSX_SYSROOT=macosx`, set before `project()`) unless `SDKROOT` or `-DCMAKE_OSX_SYSROOT` says otherwise — Apple clang's own fallback is the Command Line Tools' SDK, which can be newer than the selected Xcode's linker understands (2026-09-19). The Windows presets are **MinGW-w64** builds (user decision, 2026-09-19): Ninja over the toolchain on PATH in an MSYS2 shell — GCC in the UCRT64 environment on x64, clang in CLANGARM64 on arm64 — linked `-static` and against a static libcurl (`CURL_USE_STATIC_LIBS`; `cmake/ApogeeDependencies.cmake` reads curl's static dependency closure from pkg-config and puts it on the imported target, resolving the alias curl's own config file makes of it), so the executable ships alone with no runtime DLL beside it. `cicd.sh` refuses non-native targets, deferring them to the CI matrix, or failing on them with `--no-defer`; a macOS host can still cross-compile the x64 one for a compile check with Homebrew's `mingw-w64`: a toolchain file plus `-DCMAKE_C_COMPILER=x86_64-w64-mingw32-gcc -DCMAKE_CXX_COMPILER=x86_64-w64-mingw32-g++` on the command line, the latter because the preset's own compiler entries would otherwise win.

### Commands

Run from `lib/src/cli`, or with `make -C lib/src/cli <target>` from anywhere.

| Command | Does |
|---|---|
| `lib/scripts/cicd.sh --test` | **The repo-wide entry point.** Builds every app for the host target and runs its suite — the developer's gate before a push, and since 2026-09-22 CI's first stage too. `--platform`, `--fresh`, `--clean`, `--jobs` too; `--no-defer` turns a target this host cannot build from a deferral into a failure (every CI runner passes it); `--clone-llama` proves the llama.cpp pin resolves and stops (one of the two first-stage CI jobs; the other is `--test`, which since 2026-09-22 runs the suite per platform with llama.cpp off); `APOGEE_CMAKE_ARGS` appends configure flags (a toolchain file, e.g. a MinGW cross-compile from macOS). |
| `make test` | The same thing for the CLI alone (it calls `cicd.sh`). |
| `make build [PRESET=…]` | Configure and build one preset. |
| `make install [PREFIX=…]` | Build, then install the binary to `$PREFIX/bin` (default `~/.local`, so no sudo). |
| `make format` / `make format-check` | Apply / verify formatting across `source/` and `tests/`. |
| `make lint` | clang-tidy over `source/` and `tests/`, including the ownership gate. |
| `make no-llama` | The fast developer build: everything except llama.cpp, which is on by default and most of the compile time. |
| `make clean` / `make fresh` | Drop a build dir / clean-room clone-and-build. |

`make install` is a thin caller into the `install()` rule in `source/CMakeLists.txt`, invoked with **`--component apogee`**. That flag is not optional: `FetchContent_MakeAvailable` adds every dependency's own `install()` rules to this project, so a component-less install also writes replxx's headers, library, and CMake package config into the prefix — and our `FIND_PACKAGE_ARGS` then prefers that broken installed copy on the next configure, so the project stops building. `cli.install_is_ours_only` guards both halves. It installs the executable (`apogee_core` is an implementation detail and is deliberately not installed), then the shell completions, and then runs **`apogee check --fix`** so the binary creates and verifies its own data directory. That last step is the parity rule in one line: the Makefile does not know what `~/.apogee/` contains, and neither installer does — all three reach the single declaration in `source/harness/layout.h`. See CLAUDE.md → *One layout declaration, and every install path reads it*.

`make lint` configures its own `build/lint` directory using the compiler from clang-tidy's own directory. That is not incidental: on macOS the normal build uses Apple clang, whose libc++ headers Homebrew's clang-tidy cannot find, and every file fails to parse with a misleading `'cstddef' file not found`.

### Options

| Option | Default | Meaning |
|---|---|---|
| `APOGEE_BUILD_TESTS` | `ON` | Build the Catch2 suite. |
| `APOGEE_ENABLE_LLAMA` | `ON` | Build the pinned llama.cpp and the real `LlamaRuntime` behind it. **ON by default since 2026-09-19** (user decision: local inference ships with the tool, so every platform build, every CI test run and every release build links it — until then the default was OFF, one CI job carried the option, and no shipped binary had the real runtime in it). Heavy (GPU kernels), so `make no-llama` is the developer's fast build. Built for the platform, not the machine: `GGML_NATIVE` off, OpenMP off, the Metal library embedded, and `BUILD_SHARED_LIBS` forced off project-wide because llama.cpp's own default had been leaking ON into the cache (see `third_party/CMakeLists.txt` and the top-level `CMakeLists.txt`). With it OFF, `backends/llama_real.cpp` compiles to the "not built in" answer and the llamacpp backend refuses construction with a message saying how to enable it — everything else about the provider is still built and tested. |

---

## Dependencies

**Strategy: FetchContent**, every declaration carrying `FIND_PACKAGE_ARGS` so a system-installed copy wins and the network fetch is the fallback, and `SYSTEM` so third-party headers are not held to Apogee's warning bar. All declarations live in one file: [`lib/src/cli/cmake/ApogeeDependencies.cmake`](../../src/cli/cmake/ApogeeDependencies.cmake).

| Concern | Pick | Status |
|---|---|---|
| JSON | nlohmann/json `v3.11.3` | Wired |
| YAML | yaml-cpp `0.8.0` | Wired — **read path only.** The config engine never serializes through it (see `harness/`). Its CMakeLists predates a CMake policy removal, so `CMAKE_POLICY_VERSION_MINIMUM` is raised around its `FetchContent_MakeAvailable` and restored immediately, and its `emitterutils.cpp` uses `uint16_t` without `<cstdint>` (libstdc++ 13+ stops pulling it in; GCC 15/16 and the MinGW-w64 compilers fail on it), so the vendored target is compiled with `-include cstdint`; both are fixed on its master — revisit when it cuts a release past 0.8.0. |
| CLI parsing + completions | CLI11 `v2.4.2` | Wired |
| Chat line editing | replxx `release-0.0.4` | Wired. Used only by the chat REPL's interactive path; a piped run never constructs it. GNU readline was ruled out on licence grounds (GPL). |
| Tests | Catch2 `v3.7.1` | Wired (only when `APOGEE_BUILD_TESTS`) |
| HTTP client | libcurl (system) | Wired 2026-08-26. **Found, never fetched** — building it from source would mean choosing a TLS stack too, and the point of the platform-trust-store decision is to use the one the OS already manages. Note the trap recorded in `ApogeeDependencies.cmake`: `CURL_INCLUDE_DIRS` is the macOS SDK's own `/usr/include`, and propagating it as `-isystem` breaks any mixed-toolchain build (i.e. `make lint`); the redundant entry is stripped from the imported target. |
| Local inference | llama.cpp, pinned to `549b9d84` | `third_party/`, off by default |
| HTTP server | cpp-httplib `v0.56.0`, **fetched not found** | Wired 2026-09-13 for `apogee serve`. Header-only, included by `httpserver/serve.cpp` alone. Fetched deliberately, like SQLite: a system copy is a compiled library built with whatever TLS and compression options its packager chose, and the server must not gain a TLS stack on one machine and not another — so every optional integration (OpenSSL, zlib, brotli, zstd) is switched off explicitly and the binary's dependency set is the same on all five targets. Its non-blocking resolver is off too (2026-09-19): a per-platform code path that on Windows calls `GetAddrInfoExCancel`, which the MinGW-w64 headers do not declare, and that only httplib's *client* would use — Apogee's client is curl, and `serve` binds rather than resolves. Plain HTTP; a deployment terminates TLS in front of it. |
| JSON Schema | pboettch/json-schema-validator `2.3.0`, fetched and pinned with `FIND_PACKAGE_ARGS` | Wired 2026-09-13 for structured agent output: draft-07 validation of a report on every provider, whatever native mode the wire also asked for. It sits on nlohmann/json — the project's JSON library — which is why it was picked over a second JSON stack; a hand-rolled validator would be a second draft-07. Included by `agentloop/structured.cpp` alone; its tests, examples and install rules are off. |
| Chunk store + lexical search | SQLite `3.53.4` amalgamation, **fetched not found** | `third_party/`, wired 2026-09-07. Fetched deliberately, against the project's find-first default: Windows ships no SQLite at all, and **FTS5 is a compile-time flag** — a system copy built without it fails at the `CREATE VIRTUAL TABLE`, on the user's machine, after a corpus has been ingested. Built as `apogee_sqlite3` with `SQLITE_ENABLE_FTS5` and `SQLITE_DQS=0` (so a bare double-quoted string is an error, not a silent identifier). Adding a C amalgamation is why the top-level `project()` declares `LANGUAGES CXX C`. |

These picks were standardized once, project-wide. **Downstream work consumes them rather than reopening them** — a second JSON library or a second CLI parser is a bug, not a preference.

### Adding a dependency

1. Declare it in `lib/src/cli/cmake/ApogeeDependencies.cmake` with a **pinned** `GIT_TAG` (never a branch), plus `SYSTEM` and `FIND_PACKAGE_ARGS`.
2. `FetchContent_MakeAvailable` it there.
3. `target_link_libraries` in the consuming target's `CMakeLists.txt`.
4. Record it in the table above.

Vendored code goes under `lib/src/cli/third_party/` instead, and is **never edited in-tree** — see [its README](../../src/cli/third_party/README.md). Fix upstream and move the pin, or wrap it in a first-party boundary class.

---

## Testing

`make -C lib/src/cli test`, or `ctest --test-dir lib/src/cli/build/<preset> --output-on-failure` against an existing build. A single case: add `-R "<name regex>"`, or run the `apogee_tests` binary directly with a Catch2 test name or `[tag]`.

---

## Adding a new extension point

- **Adding a subcommand** — see [`commands/` above](#commands--the-subcommand-scaffold).
- **Adding a provider backend** — recipe lands with the `harness-core` item, once that seam exists.

---

## Cutting a release

**A merge into `stable` is a release** (user decision, 2026-09-22). The version in `project(... VERSION x.y.z)` — `lib/src/cli/CMakeLists.txt`, the one place it lives — decides which one, and whether there is one at all. Nothing else needs doing.

```
bump VERSION on the branch  ->  PR  ->  merge  ->  release published
```

[`release.yml`](../../../.github/workflows/release.yml) runs on the push to `stable`. Its `gate` job reads `CMakeLists.txt`, and if that version has no release yet, the five builds run and `publish` creates **the tag and the release together**. If it does have one, the run stops at the gate and nothing is published — which is how a docs or hotfix merge into `stable` declines to cut a release, with no exception rule required.

### What you actually do

1. **Bump `project(... VERSION x.y.z)`** on the version branch, committed with the rest of the release.
2. **Open the PR.** CI's `version bump` check fails if that version is already released — that is the moment a forgotten bump surfaces, rather than after the merge when the release silently does not happen.
3. **Merge.** The release publishes itself.
4. **Open the next dev branch** from `stable`, named for the release being *built*:
   ```sh
   git checkout stable && git pull && git checkout -b vX.Y.Z+1 && git push -u origin vX.Y.Z+1
   ```

### The manual path

Still supported, as the escape hatch — for re-cutting a release, or for tagging a commit that is not the tip of a merge:

```sh
make -C lib/src/cli release VERSION=x.y.z
```

Preflights the tree, branch, version and tag, asks once, then tags and pushes; the tag push triggers the same workflow. `make -C lib/src/cli release-check VERSION=x.y.z` runs the checks and pushes nothing. `VERSION` is the only required input; `REMOTE`, `RELEASE_BRANCH` and `CONFIRM=yes` are the overrides.

### The version guard, and where it lives

A tag whose name disagrees with `CMakeLists.txt` would publish a release whose binary reports a different version, and the build would stay green throughout — the pipeline's "Verify the staged binary runs" step runs `apogee version` but never reads what it printed. That hole is now closed in **two** places: `make release` refuses locally, and `release.yml`'s `gate` job fails the run before any runner starts. The gate is the one that cannot be skipped; a hand-pushed tag still meets it.

### No orphaned tags

`publish` creates the tag and the release in a single `gh release create --target <sha>`, and nothing earlier in the workflow pushes a tag. A blocking build failure therefore leaves nothing behind: fix the build and merge again, with no tag to delete first. This is the one real behavioural difference from the old flow, where the tag push was what started everything and a failed build stranded it.

### Dry run

`make release-check VERSION=x.y.z` is the local half — tree and version, no remote. For the pipeline, use the `workflow_dispatch` with `dry_run` (default on): Actions → Release → "Run workflow". A dispatch always builds, even when the version is already released, so a dry run is never silently skipped; only `publish` is. All five archives land as Actions artifacts to inspect. Worth doing whenever the pipeline itself changed.

### Pitfall: never name a branch after a tag

If both `refs/heads/vX.Y.Z` and `refs/tags/vX.Y.Z` exist, git cannot resolve the short name and every push fails with:

```
error: src refspec vX.Y.Z matches more than one
```

`git checkout vX.Y.Z` also warns and picks the branch. Version-named *branches* are for releases still being built; a finished release is a tag and only a tag. Pushing as `refs/tags/vX.Y.Z` is immune either way, which is why `make release` spells it out.

### Re-cutting a bad release

Delete the release and its tag, fix, and merge or tag again:

```sh
gh release delete vX.Y.Z --cleanup-tag
```

`gh` ([cli.github.com](https://cli.github.com)) is not needed to cut a release — the pipeline uses its own `github.token` on the runner — but it is the only way to delete or repair one from a terminal, and `gh run watch` follows a build live.
